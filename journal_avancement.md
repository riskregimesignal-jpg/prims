# Journal d'avancement de Prims

Dernière mise à jour : 29 mars 2026

## Étapes terminées
- [x] 0.1 : Installation de Rust, Git, VS Code (vérifié)
- [x] 0.2 : Création du dépôt GitHub et premier push (réglé problème permission : ajout de manuios225 comme collaborateur)
- [x] 0.3 : Initialisation du projet Rust `prims` et création de la structure de dossiers conforme à la roadmap
- [x] 0.4 : Ajout des dépendances de base dans `Cargo.toml` (`anyhow`, `thiserror`, `serde`, `serde_json`, `bincode`, `log`, `env_logger`, `tokio`, `clap`, `criterion`)
- [x] 0.5 : Vérification de `cargo build` et `cargo run`
- [x] 0.6 : Sécurité : installation de `cargo-audit`, exécution de `cargo audit`, ajout du workflow GitHub Actions pour les audits automatiques
- [x] 0.7 : Rédaction du `README.md`
- [x] 1.1 : Ajout de `libp2p` avec les features réseau nécessaires (`tcp`, `noise`, `yamux`, `mdns`, `gossipsub`, `kad`, `request-response`, `tokio`, `macros`)
- [x] 1.2 : Création du module réseau de base et démarrage d’un nœud `libp2p` avec affichage du `PeerId`
- [x] 1.3 : Découverte de pairs validée avec mDNS + seed node local + Kademlia initiale
- [x] 1.4 : Gossipsub validé avec abonnement au topic `prims-testnet` et propagation du message de test `hello-prims`
- [x] 1.5 : Messages personnalisés du protocole réseau définis dans `src/network/message.rs` (`Ping`, `Pong`, `NewTransaction`, `NewBlock`, `GetBlocks`, `Blocks`, `Vote`) avec sérialisation `bincode` et test unitaire validé
- [x] 1.6 : Gestion des connexions ajoutée avec limites, reconnexion et bannissement temporaire des pairs malveillants (`src/network/connection.rs`, intégration dans `src/network/config.rs` et `src/network/node.rs`, tests unitaires validés)
- [x] 1.7 : Script `scripts/run_local_cluster.sh` créé et validé avec 3 nœuds locaux (`7001`, `7002`, `7003`), découverte de pairs confirmée et propagation du message `hello-prims-cluster` vérifiée
- [x] 1.8 : Benchmark de propagation validé avec instrumentation des timestamps dans `src/main.rs` ; message `hello-prims-cluster` propagé entre 3 nœuds avec une latence mesurée de `1 ms` (< 500 ms)
- [x] 1.9 : Test de résistance au spam de connexions (DoS) validé avec `scripts/test_connection_spam.sh` ; 60 tentatives TCP sur `7001`, 60 connexions acceptées au niveau TCP, 0 échec TCP, et le nœud `libp2p` a rejeté proprement les handshakes invalides via `IncomingConnectionError` sans planter
- [x] 2.1 : Structures de base sérialisables ajoutées dans `src/blockchain/types.rs` (`Transaction`, `BlockHeader`, `Block`, `Account`), module exporté via `src/blockchain/mod.rs` et `src/lib.rs`, test `blockchain::types::tests::block_structures_roundtrip_with_bincode` validé
- [x] 2.2 : Calcul du hash SHA-256 et du Merkle root implémentés dans `src/blockchain/hash.rs`, module exporté via `src/blockchain/mod.rs`, tests `merkle_root_empty_transactions_matches_sha256_empty`, `merkle_root_single_transaction_matches_transaction_hash` et `block_header_hash_is_deterministic` validés
- [x] 2.3 : Dépendance `rocksdb` ajoutée dans `Cargo.toml`, module `src/storage/mod.rs` créé avec l’abstraction `StorageBackend` et l’implémentation `RocksDbStorage`, exporté via `src/lib.rs`, test `storage::tests::opens_rocksdb_storage` validé
- [x] 2.4 : Préfixes de clés définis dans `src/storage/keys.rs` pour `b:{height}`, `h:{hash}`, `t:{hash}`, `a:{address}`, `s:{address}`, `c:{address}`, `m:{address}:{key}`, module exporté via `src/storage/mod.rs`, test `storage::keys::tests::storage_key_prefixes_match_roadmap` validé
- [x] 2.5 : Fonctions de base `put`, `get`, `delete`, `iter` implémentées dans `src/storage/mod.rs` avec gestion d’erreurs, test `storage::tests::storage_put_get_delete_and_iter` validé
- [x] 2.6 : Méthodes de stockage métier `save_block`, `get_block`, `save_transaction`, `get_transaction`, `update_account`, `get_account` implémentées dans `src/storage/mod.rs`, avec test `storage::tests::storage_high_level_block_transaction_account_roundtrip` validé
- [x] 2.7 : Test unitaire de persistance et de reprise après redémarrage validé avec `storage::tests::storage_persists_after_restart` dans `src/storage/mod.rs`
- [x] 2.8 : Benchmark `storage_benchmark` validé pour l’écriture/lecture de 10 000 blocs avec `criterion` dans `benches/storage_benchmark.rs` ; mesures observées : écriture `storage_write_10k_blocks` ≈ 81.491–87.848 ms et lecture `storage_read_10k_blocks` ≈ 14.040–15.869 ms
- [x] 2.9 : Sécurité des données critiques renforcée avec `src/storage/checksum.rs`, module exporté via `src/storage/mod.rs`, tests `checksum_roundtrip_preserves_payload` et `checksum_detects_corruption` validés
- [x] 3.1 : Génération de paires de clés ed25519 implémentée dans `src/crypto/mod.rs` avec `ed25519-dalek` + `rand`, module exporté via `src/lib.rs`, test `crypto::tests::generate_ed25519_keypair_returns_32_byte_keys` validé
- [x] 3.2 : `sign_transaction` et `verify_transaction` implémentées dans `src/crypto/mod.rs` en signant tous les champs sauf `signature`, tests `sign_and_verify_transaction_roundtrip` et `verify_transaction_fails_when_payload_changes` validés
- [x] 3.3 : hash SHA-256 et `merkle_root` finalisés dans `src/blockchain/hash.rs`, avec couverture de tests de base et intégration dans les structures blockchain
- [x] 3.4 : validation des blocs implémentée dans `src/blockchain/validation.rs` avec vérification de `previous_hash`, `merkle_root` et de la signature du validateur, module exporté via `src/blockchain/mod.rs`, tests `validate_block_accepts_valid_block`, `validate_block_rejects_invalid_previous_hash`, `validate_block_rejects_invalid_merkle_root` et `validate_block_rejects_invalid_validator_signature` validés
- [x] 3.5 : anti-replay implémenté dans `src/blockchain/validation.rs` avec `validate_transaction_nonce`, vérification que le nonce de transaction est strictement supérieur au dernier nonce stocké pour le compte, tests `validate_transaction_nonce_accepts_first_transaction_for_new_account`, `validate_transaction_nonce_accepts_strictly_greater_nonce`, `validate_transaction_nonce_rejects_same_nonce` et `validate_transaction_nonce_rejects_lower_nonce` validés
- [x] 3.6 : vérification du solde suffisant et des montants positifs ajoutée dans `src/blockchain/validation.rs` avec `validate_transaction_balance`, contrôle de `amount > 0`, détection d’overflow sur `amount + fee` et refus si le solde du compte est insuffisant, tests `validate_transaction_balance_accepts_sufficient_balance`, `validate_transaction_balance_accepts_exact_balance`, `validate_transaction_balance_rejects_zero_amount`, `validate_transaction_balance_rejects_insufficient_balance`, `validate_transaction_balance_rejects_missing_account_with_zero_balance` et `validate_transaction_balance_rejects_overflow` validés
- [x] 3.7 : limites de taille ajoutées dans `src/blockchain/validation.rs` avec `MAX_TRANSACTION_SIZE_BYTES = 1_048_576`, `MAX_BLOCK_SIZE_BYTES = 10_485_760`, `validate_transaction_size` et `validate_block_size`, intégration du contrôle de taille dans `validate_block`, tests `validate_transaction_size_accepts_small_transaction`, `validate_transaction_size_rejects_oversized_transaction`, `validate_block_size_accepts_small_block` et `validate_block_size_rejects_oversized_block` validés
- [x] 3.8 : tests de signature, vérification et corruption validés avec `cargo test crypto::tests` et `cargo test blockchain::validation::tests`, couvrant la signature ed25519, la vérification, l’altération des transactions signées et les rejets de blocs / transactions corrompus ou surdimensionnés
- [x] 3.9 : tests de sécurité ajoutés dans `src/blockchain/hash.rs` pour vérifier le déterminisme de SHA-256, la différence de hash sur entrées distinctes, le changement de hash lors d’une altération de transaction et le changement de `merkle_root` quand l’ordre des transactions change, tests `sha256_is_deterministic_for_same_input`, `sha256_differs_for_distinct_inputs`, `hash_transaction_changes_when_payload_changes` et `merkle_root_changes_when_transaction_order_changes` validés
- [x] 4.1 : structure `Validator { address, stake, locked_until }` ajoutée dans `src/blockchain/types.rs`, stockage des validateurs implémenté dans `src/storage/mod.rs` avec `save_validator` et `get_validator` via la clé `stake_key`, tests `validator_structure_roundtrip_with_bincode`, `saves_and_reads_validator` et `persists_validator_after_restart` validés
- [x] 4.2 : transactions spéciales ajoutées dans `src/blockchain/types.rs` avec `TransactionType::{Transfer, Stake { duration }, Unstake}`, intégration dans `Transaction`, tests `special_transactions_roundtrip_with_bincode`, `block_structures_roundtrip_with_bincode` et `crypto::tests` validés
- [x] 4.3 : sélection du proposant implémentée dans `src/consensus/mod.rs` avec `seed = sha256(previous_block_hash + height)`, choix pondéré proportionnel au stake, filtrage des validateurs à stake nul, ordre déterministe par adresse, module exporté via `src/lib.rs`, tests `select_proposer_rejects_empty_validator_set`, `select_proposer_ignores_zero_stake_validators`, `select_proposer_by_seed_respects_weighted_ranges`, `select_proposer_is_deterministic_for_same_inputs` et `select_proposer_is_independent_of_input_order` validés
- [x] 4.4 : proposition de bloc implémentée avec `Mempool` et `propose_block` dans `src/consensus/mod.rs`, signature de l’en-tête du bloc par le proposant, sélection d’un nombre limité de transactions depuis la mempool, diffusion réseau préparée via `publish_message` / `publish_proposed_block` dans `src/network/node.rs` et topics `prims.blocks` / `prims.transactions` dans `src/network/gossip.rs`, tests `propose_block_builds_signed_block_from_mempool`, `propose_block_respects_transaction_limit_and_keeps_rest_in_mempool` et `blockchain_block_to_network_block_preserves_height_and_transactions` validés
- [x] 4.5 : votes signés implémentés dans `src/consensus/mod.rs` avec `ConsensusVote`, `create_signed_vote`, `verify_vote`, `VoteCollector` et `VoteTally`, collecte et agrégation pondérées par le stake, diffusion réseau préparée via `publish_vote` dans `src/network/node.rs` et topic `prims.votes` dans `src/network/gossip.rs`, tests `create_and_verify_vote_roundtrip`, `verify_vote_fails_when_payload_changes`, `vote_collector_aggregates_weighted_votes`, `vote_collector_rejects_duplicate_vote_from_same_validator`, `vote_collector_rejects_unknown_validator`, `vote_collector_rejects_invalid_signature` et `consensus_vote_to_network_vote_preserves_signature_and_payload` validés
- [x] 4.6 : finalisation implémentée dans `src/consensus/mod.rs` avec `total_active_stake` et `is_block_finalized`, règle stricte `approve_stake > 2/3 du stake actif`, tests `block_is_not_finalized_at_exact_two_thirds`, `block_is_finalized_above_two_thirds_of_weighted_votes` et `block_is_not_finalized_when_no_active_stake_exists` validés
- [x] 4.7 : gestion des forks implémentée dans `src/consensus/mod.rs` avec `ChainFork`, `cumulative_chain_weight` et `choose_heaviest_chain`, règle de sélection de la chaîne la plus lourde en stake cumulé avec départage par hauteur puis hash, tests `cumulative_chain_weight_sums_approve_stake`, `choose_heaviest_chain_prefers_higher_cumulative_stake`, `choose_heaviest_chain_breaks_tie_with_height_then_hash` et `choose_heaviest_chain_returns_none_for_empty_input` validés
- [x] 4.8 : slashing implémenté dans `src/consensus/mod.rs` avec `DoubleVoteProof`, `detect_double_vote` et `slash_validator_for_double_vote`, détection des double-votes par preuve (même validateur, même hauteur, `block_hash` différent) et pénalité déterministe sur le stake, tests `detect_double_vote_accepts_valid_conflicting_votes`, `detect_double_vote_rejects_same_block_hash`, `detect_double_vote_rejects_different_heights`, `detect_double_vote_rejects_invalid_signature`, `slash_validator_for_double_vote_reduces_stake`, `slash_validator_for_double_vote_rejects_invalid_penalty_bps` et `slash_validator_for_double_vote_rejects_mismatched_validator` validés
- [x] 4.9 : récompenses implémentées dans `src/consensus/mod.rs` avec `ANNUAL_INFLATION_BPS = 200`, `SECONDS_PER_YEAR`, `ValidatorReward`, `total_transaction_fees`, `annualized_inflation_reward` et `distribute_voting_rewards`, distribution des frais et de l’inflation annuelle de 2% aux validateurs ayant effectivement voté, tests `total_transaction_fees_sums_fees_across_transactions`, `annualized_inflation_reward_matches_two_percent_for_one_year`, `distribute_voting_rewards_splits_pool_across_voting_validators_only` et `distribute_voting_rewards_rejects_empty_vote_set` validés
- [x] 4.10 : test global de simulation ajouté dans `src/consensus/mod.rs` avec `four_validator_simulation_covers_selection_fork_and_slashing`, couvrant 4 validateurs, la sélection déterministe du proposant, la finalisation du fork gagnant, le rejet du fork perdant et le slashing d’un validateur en double-vote, test validé
- [x] 4.11 : benchmark de finalisation ajouté dans `src/consensus/mod.rs` avec `finalization_benchmark_completes_under_two_seconds`, scénario complet de proposition + votes + finalisation mesuré avec `std::time::Instant`, objectif de finalisation < 2 secondes validé
- [x] 4.12 : test de sécurité byzantin ajouté dans `src/consensus/mod.rs` avec `byzantine_thirty_three_percent_attack_does_not_break_finalization`, scénario à 3 validateurs (34/33/33) validant qu’un bloc honnête à 67% finalise, qu’un fork byzantin à 33% ne finalise pas et que `choose_heaviest_chain` conserve la chaîne honnête, tests ciblé et complet `consensus::tests` réussis
- [x] 5.1 : partitions logiques de mempool introduites dans `src/consensus/mod.rs` avec `Mempool { partition_count, partitions }`, répartition déterministe par adresse source via hash de `transaction.from` modulo `N`, utilisation par défaut de `std::thread::available_parallelism()` pour aligner `N` sur le nombre de cœurs logiques disponibles, tests `mempool_uses_explicit_partition_count`, `mempool_keeps_same_sender_fifo_within_a_partition`, `cargo test consensus --lib` et `cargo test` validés
- [x] 5.2 : chaque partition de mempool est désormais gérée par une tâche asynchrone dans `src/consensus/mod.rs`, avec `MempoolPartition`, `MempoolCommand`, files `tokio::sync::mpsc`, réponses `oneshot` et runtime interne `Arc<Runtime>` ; la collecte et l’extraction des transactions passent désormais par ces files par partition ; tests `mempool_uses_explicit_partition_count`, `mempool_keeps_same_sender_fifo_within_a_partition`, `mempool_pending_and_take_for_block_work_across_partition_tasks` et `cargo test` complets validés
- [x] 5.3 : répartiteur (dispatcher) branché dans `src/main.rs` pour les messages réseau entrants `Message::NewTransaction` ; le nœud décode désormais `Message::from_bytes(&message.data)`, désérialise `network_transaction.payload` en transaction métier avec `bincode`, puis aiguille la transaction vers la bonne partition via `mempool.add_transaction(transaction)` ; sauvegarde créée `src/main.rs.step-5.3.bak`, build `cargo build --bin prims` validé et régression globale `cargo test` validée avec `87 passed; 0 failed`
- [x] 5.4 : les proposants de blocs (pour l’instant un seul shard) peuvent désormais piocher dans toutes les partitions de la mempool via `Mempool::take_for_block`, avec couverture explicite ajoutée dans `src/consensus/mod.rs` par le test `propose_block_can_pull_transactions_from_all_partitions` ; test ciblé validé et régression globale `cargo test` validée avec `88 passed; 0 failed`
- [x] 5.5 : pas de priorité par frais ; frais fixes imposés via `FIXED_TRANSACTION_FEE` dans `src/blockchain/types.rs`, validation des transactions alignée sur ce frais fixe dans `src/blockchain/validation.rs`, helpers/tests ajustés dans `src/consensus/mod.rs`, `src/crypto/mod.rs`, `src/network/node.rs`, `src/storage/mod.rs` et `src/blockchain/hash.rs` ; test de répartition des récompenses rendu déterministe ; `cargo test` global validé avec 88 tests passants et 0 échec
- [x] 5.6 : rate limiting appliqué dans `src/consensus/mod.rs` avec `MAX_TRANSACTIONS_PER_ADDRESS_PER_BLOCK = 100` lors de `TakeForBlock` ; nouveau test `mempool_limits_sender_to_one_hundred_transactions_per_block` validé ; `cargo test` global validé avec 89 tests passants et 0 échec
- [x] 5.7 : script de charge `scripts/test_transaction_load.sh` ajouté et validé avec `prims-cli flood`, helper `publish_transaction` dans `src/network/node.rs`, réception réseau asynchrone via `mempool.add_transaction_async(transaction).await` dans `src/main.rs`, et suppression du `mempool.len()` synchrone dans le log de dispatch pour éviter un panic Tokio ; test de charge validé en local avec `4` clients concurrents × `1000` transactions = `4000` transactions demandées, `4000` publiées, `4000` injectées dans la mempool, `0` client en échec, durée observée ≈ `11 s`
- [x] 5.8 : benchmark local validé au-dessus de l’objectif de la roadmap avec optimisation du script de charge, build `--release`, désabonnement Gossipsub des clients CLI sur les topics inutiles, suppression/épuration des logs coûteux, ajout d’un retry sur `AllQueuesFull`, et mesure fine en millisecondes ; résultat validé : `14` clients concurrents × `5000` transactions = `70000` transactions demandées, `70000` publiées, `70000` injectées dans la mempool, `0` client en échec, durée observée `6,668 s`, soit `10497,90 TPS` en local
- [x] 5.9 : test de sécurité ajouté dans `src/consensus/mod.rs` avec `propose_block_does_not_preserve_global_submission_order_preventing_sandwich_attacks`, vérifiant qu’un bloc proposé ne préserve pas un ordre global de soumission visible exploitable pour une attaque sandwich ; `cargo test --lib` et `cargo test` validés avec 90 tests passants côté bibliothèque et 0 échec global
- [x] 6.1 : module `src/sharding/mod.rs` créé avec `ShardingConfig`, `DEFAULT_INITIAL_SHARD_COUNT = 64`, validation d’un nombre de shards puissance de 2 et évolution future via `apply_governance_update`, module exporté via `src/lib.rs`, tests `default_sharding_config_uses_64_shards`, `custom_shard_count_must_be_a_power_of_two` et `governance_can_update_shard_count_to_another_power_of_two` validés ; `cargo test --lib` validé avec `93 passed; 0 failed` et `cargo test` global validé sans échec
- [x] 6.2 : beacon chain ajoutée dans `src/sharding/mod.rs` avec `BeaconChain`, `ShardCommitteeAssignment` et `ShardStateRoot` ; gestion des validateurs actifs, assignation pseudo-aléatoire déterministe des comités par epoch et stockage / mise à jour des racines d’état des shards ; tests `sharding::tests` validés, `cargo test --lib` validé avec `98 passed; 0 failed` et `cargo test` global validé sans échec
- [x] 6.3 : consensus propre à chaque shard ajouté dans `src/sharding/mod.rs` avec `ShardConsensus` et `ShardConsensusError`, en réutilisant la logique de consensus de la phase 4 via `ConsensusVote`, `VoteCollector`, `VoteTally`, `VoteError` et `is_block_finalized` ; ajout des helpers `validators_for_shard` et `consensus_for_shard` pour construire le comité effectif d’un shard à partir de la beacon chain ; tests `beacon_chain_builds_shard_consensus_from_committee`, `shard_consensus_finalizes_block_with_its_committee_votes` et `shard_consensus_rejects_vote_from_validator_outside_committee` validés ; `cargo test sharding::tests --lib` passe avec `11 passed`, `cargo test --lib` passe avec `101 passed; 0 failed`, et `cargo test` global est valide sans échec
- [x] 6.4 : transactions cross-shard implémentées dans `src/sharding/mod.rs` avec `CrossShardExecution` et `CrossShardTransactionError`, protocole en trois phases `prepare_cross_shard_transaction` → `validate_cross_shard_transaction` → `commit_cross_shard_transaction`, génération de `CrossShardReceipt` / preuves déterministes et helper `include_receipts_in_block` ; extension des structures blockchain dans `src/blockchain/types.rs` avec `source_shard`, `destination_shard`, `CrossShardPhase`, `CrossShardReceipt`, `Block.receipts` et `Transaction::is_cross_shard`, ajustement de compatibilité dans `src/blockchain/validation.rs`, `src/consensus/mod.rs`, `src/network/node.rs`, `src/storage/mod.rs`, `src/crypto/mod.rs` et `src/bin/prims-cli.rs` ; tests `prepare_rejects_same_shard_transaction`, `cross_shard_transaction_prepare_validate_commit_generates_receipts`, `cross_shard_commit_requires_destination_validation` et `receipts_can_be_included_in_block` validés ; `cargo test sharding::tests --lib` passe avec `15 passed`, `cargo test --lib` passe avec `106 passed; 0 failed`, `cargo test` global est valide sans échec et sans warning.
- [x] 6.5 : la beacon chain valide désormais les preuves cross-shard et la finalité globale dans `src/sharding/mod.rs` avec `validate_cross_shard_receipt`, `validate_cross_shard_execution_proofs`, `is_cross_shard_execution_globally_finalized` et de nouvelles erreurs explicites `InvalidReceiptCount`, `MissingReceiptPhase`, `ReceiptTxHashMismatch`, `ReceiptRouteMismatch` et `InvalidReceiptProof` ; tests `beacon_chain_validates_cross_shard_proofs_and_global_finality`, `cross_shard_global_finality_is_false_until_commit_phase` et `beacon_chain_rejects_tampered_cross_shard_receipt_proof` validés ; `cargo test sharding::tests --lib` passe avec `18 passed`, `cargo test --lib` passe avec `109 passed; 0 failed`, et `cargo test` global est valide sans échec.
- [x] 6.6 : la beacon chain met désormais à jour l’état des shards depuis les blocs finalisés dans `src/sharding/mod.rs` avec `update_shard_state_root_from_block`, en réutilisant le `merkle_root` du bloc comme nouvelle racine d’état et en conservant correctement les racines par epoch ; tests `beacon_chain_updates_shard_state_root_from_block` et `beacon_chain_tracks_shard_state_updates_across_epochs` validés ; `cargo test sharding::tests --lib` passe avec `20 passed`, `cargo test --lib` passe avec `111 passed; 0 failed`, et `cargo test` global est valide sans échec.
- [x] 6.7 : simulation Docker multi-shards validée avec `Dockerfile` multi-stage et `docker-compose.yml` (services `shard1`, `shard2`, `shard3` exposés sur `7001`, `7002`, `7003`), construction réussie des images `prims-shard1`, `prims-shard2` et `prims-shard3`, exécution confirmée par `docker compose up -d` et `docker compose ps`, et découverte inter-pairs confirmée dans les logs Docker via `Discovered(...)` et `NewExternalAddrOfPeer(...)` malgré des seeds `/dns4/...` encore rejetées en `MultiaddrNotSupported(...)`
- [x] 6.8 : benchmark multi-shards ajouté avec `scripts/benchmark_shards_tps.sh` et validé sur Docker pour `1`, `2` et `3` shards ; métrique principale retenue : `published_tps` ; résultats observés : `53.73 TPS` sur `1` shard (`400` transactions publiées en `7.445 s`), `112.55 TPS` sur `2` shards (`800` en `7.108 s`) et `169.35 TPS` sur `3` shards (`1200` en `7.086 s`) ; `dispatched_tps` conservé comme métrique de diagnostic (`0.00`, `56.27`, `112.90`) car `shard1` ne journalise pas encore `Dispatched transaction` dans ce setup Docker ; artefacts enregistrés dans `logs/benchmark_shards_tps/20260319_235957/` et `~/Documents/prims_sources/step-6.8/`
- [x] 6.9 : test de sécurité ajouté dans `src/sharding/mod.rs` avec `compromised_shard_state_root_does_not_affect_other_shards`, vérifiant qu’une compromission simulée d’un shard par modification de sa racine d’état n’altère pas les racines des autres shards ; test ciblé validé, `cargo test sharding::tests::` validé avec `21 passed; 0 failed`, et `cargo test` global validé avec `112 passed; 0 failed`
- [x] 7.1 : dépendances `ark-*` ajoutées dans `Cargo.toml` (`ark-ff`, `ark-ec`, `ark-groth16`, `ark-bls12-381`, `ark-std` en `0.5.0`) ; `cargo check` validé et `Cargo.lock` mis à jour avec les 5 crates `ark-ff`, `ark-ec`, `ark-groth16`, `ark-bls12-381` et `ark-std`
- [x] 7.2 : modèle UTXO minimal ajouté dans `src/privacy/mod.rs` avec `UtxoId`, `UtxoInput`, `UtxoOutput`, `UtxoTransaction`, validation de base (`validate_basic`) et erreurs `UtxoModelError` ; module exporté via `src/lib.rs` avec `pub mod privacy;` ; tests `utxo_transaction_consumes_inputs_and_creates_outputs`, `utxo_transaction_requires_at_least_one_input`, `utxo_transaction_requires_at_least_one_output` et `utxo_transaction_rejects_zero_value_outputs` validés ; `cargo test privacy:: --lib` OK (`4 passed; 0 failed`) et `cargo test --lib` OK (`116 passed; 0 failed`)
- [x] 7.3 : `Note` anonyme minimale ajoutée dans `src/privacy/mod.rs` avec `NoteRecipientPublicKey`, `NoteCommitment`, `Note { commitment }`, constructeur `Note::new`, calcul `Note::compute_commitment` basé sur SHA-256 (`value + recipient_public_key + blinding`) et erreurs `NoteError` pour refuser une valeur nulle, une clé publique destinataire nulle ou un facteur d’aveuglement nul ; tests `note_commitment_is_deterministic_for_same_inputs`, `note_commitment_changes_when_value_changes`, `note_creation_builds_commitment_from_value_and_recipient`, `note_creation_rejects_zero_value`, `note_creation_rejects_zero_recipient_public_key` et `note_creation_rejects_zero_blinding` validés ; `cargo test privacy:: --lib` OK (`10 passed; 0 failed`), `cargo test privacy::tests::note_ --lib` OK (`6 passed; 0 failed`) et `cargo test --lib` OK (`122 passed; 0 failed`)
- [x] 7.4 : arbre de Merkle des notes ajouté dans `src/privacy/mod.rs` avec `NoteMerkleHash`, `NoteMerkleProof`, `NoteMerkleTree` et `NoteMerkleTreeError` ; racine déterministe pour arbre vide, construction depuis des notes ou des engagements, ajout incrémental via `append`, génération de preuve d’appartenance via `generate_proof`, vérification via `verify_proof`, hachage distinct des feuilles et des nœuds internes par SHA-256 avec séparation de domaine `prims-note-merkle-*`, et duplication du dernier nœud quand un niveau contient un nombre impair d’éléments ; tests `note_merkle_tree_empty_root_is_deterministic`, `note_merkle_tree_root_is_deterministic_for_same_notes`, `note_merkle_tree_root_changes_when_notes_change`, `note_merkle_tree_append_matches_from_notes`, `note_merkle_tree_generates_valid_proof_for_existing_leaf` et `note_merkle_tree_rejects_out_of_bounds_leaf_index` validés ; `cargo test note_merkle_tree_ --lib` OK (`6 passed; 0 failed`), `cargo test privacy:: --lib` OK (`16 passed; 0 failed`), `cargo test privacy::tests::note_ --lib` OK (`12 passed; 0 failed`) et `cargo test --lib` OK (`128 passed; 0 failed`)
- [x] 7.5 : circuit zk-SNARK minimal ajouté dans `src/privacy/mod.rs` avec `NoteSpendingPrivateKey`, `ZkNoteInputWitness`, `ZkNoteOutputWitness`, `ZkTransferPublicInputs`, `ZkTransferWitness`, `ZkTransferError`, `derive_note_recipient_public_key`, `compute_note_merkle_root_from_proof`, `verify_zk_transfer_public_inputs` et `ZkTransferCircuit` (`ConstraintSynthesizer<Fr>`) ; ajout des dépendances `ark-relations`, `ark-r1cs-std` et `ark-crypto-primitives` dans `Cargo.toml` ; le circuit contraint l’existence des entrées dans l’arbre via preuve de Merkle, la connaissance d’une clé privée de dépense reliée à `NoteRecipientPublicKey` par SHA-256 avec le préfixe `prims-note-owner`, l’égalité `somme des entrées = somme des sorties + frais`, et la validité des engagements d’entrée / sortie ; tests `zk_transfer_reference_verifier_accepts_balanced_valid_witness`, `zk_transfer_reference_verifier_rejects_amount_imbalance`, `zk_transfer_reference_verifier_rejects_invalid_merkle_proof`, `zk_transfer_circuit_accepts_valid_witness` et `zk_transfer_circuit_rejects_invalid_public_output_commitment` validés ; `cargo test privacy:: --lib` OK (`19 passed; 0 failed`), `cargo test zk_transfer_reference_verifier_ --lib` OK (`3 passed; 0 failed`), `cargo test zk_transfer_circuit_ --lib` OK (`2 passed; 0 failed`) et `CARGO_INCREMENTAL=0 RUSTFLAGS="-C debuginfo=0" cargo test --lib` OK (`133 passed; 0 failed`)
- [x] 7.6 : génération d’un trusted setup simplifié documenté pour `ZkTransferCircuit` avec ajout de `ark-serialize` dans `Cargo.toml`, ajout dans `src/privacy/mod.rs` de `ZkTransferSetupCircuitMetadata`, `ZkTransferSetupError` et `build_reference_zk_transfer_setup_circuit`, création du binaire `src/bin/prims-setup.rs` (`init`, `contribute`, `finalize`, `inspect`), génération locale d’un transcript de cérémonie à 2 contributions (`manu225`, `participant-local-2`) et des artefacts `artifacts/zk-setup/ceremony_state.json`, `groth16_proving_key.bin`, `groth16_verifying_key.bin`, `setup_metadata.json`, documentation `docs/privacy_trusted_setup.md` ; métadonnées de circuit de référence fixées à `1` entrée, `2` sorties, profondeur de preuve de Merkle `1`, `2` feuilles et frais `5` ; validations réussies : `cargo check --bin prims-setup`, `cargo run --bin prims-setup -- --help`, `cargo test privacy:: --lib` OK (`21 passed; 0 failed`), `CARGO_INCREMENTAL=0 RUSTFLAGS="-C debuginfo=0" cargo test --lib` OK (`133 passed; 0 failed`) et `cargo run --bin prims-setup -- inspect --out-dir artifacts/zk-setup` confirmant `2` contributions et le digest final `b7fb50c1777b6ab09e2fcb4d4050190b68b3b908c18175229d9cfbf3b3ed35a7`
- [x] 7.7 : génération de preuve Groth16 hors chaîne et vérification côté nœud ajoutées dans `src/privacy/mod.rs` avec les aliases `ZkTransferProof`, `ZkTransferProvingKey`, `ZkTransferVerifyingKey` et `ZkTransferPreparedVerifyingKey`, l’erreur dédiée `ZkTransferProofSystemError`, et les helpers `zk_transfer_public_inputs_to_field_elements`, `prepare_zk_transfer_verifying_key`, `generate_zk_transfer_proof`, `verify_zk_transfer_proof` et `verify_zk_transfer_proof_with_prepared_key` ; l’encodage des entrées publiques a été aligné sur l’allocation arkworks via un `ConstraintSystem` pour garantir la compatibilité exacte avec la clé de vérification ; deux tests Groth16 ont été ajoutés pour valider qu’une preuve issue du circuit de référence est acceptée avec les bonnes entrées publiques et rejetée si les données publiques sont altérées (`fee` falsifié) ; `cargo test privacy:: --lib` passe avec `23 passed; 0 failed` et `CARGO_INCREMENTAL=0 RUSTFLAGS="-C debuginfo=0" cargo test --lib` passe avec `135 passed; 0 failed`.
- [x] 7.8 : type de transaction `AnonTransaction` ajouté dans `src/privacy/mod.rs` avec `merkle_root`, `fee`, `output_commitments` et `proof`, plus l’erreur dédiée `AnonTransactionError` et les helpers `new`, `public_inputs`, `decode_proof`, `verify` et `verify_with_prepared_key` ; la preuve Groth16 est sérialisée en format compressé via `ark-serialize` pour être transportée avec les engagements publics ; deux tests dédiés ont été ajoutés (`anon_transaction_verifies_with_matching_public_inputs`, `anon_transaction_rejects_tampered_fee`) pour valider qu’une transaction anonyme reconstruite depuis ses entrées publiques vérifie correctement et qu’un `fee` falsifié est rejeté ; `cargo test privacy:: --lib` passe avec `25 passed; 0 failed` et `CARGO_INCREMENTAL=0 RUSTFLAGS="-C debuginfo=0" cargo test --lib` passe avec `137 passed; 0 failed`.
- [x] 7.9 : gestion de l’état anonyme implémentée avec `AnonymousAccountState` ajouté dans `src/blockchain/types.rs` pour attacher à chaque compte un `viewing_hint` optionnel et la liste de ses engagements de notes, `ViewableNote`, `NoteViewingKey` et `NoteViewingHint` ajoutés dans `src/privacy/mod.rs` pour permettre au destinataire de détecter les notes qui lui sont destinées via un hint dérivé SHA-256 (`prims-viewing-key`), nouvelles clés RocksDB `n:`, `v:` et `r:` ajoutées dans `src/storage/keys.rs`, stockage métier étendu dans `src/storage/mod.rs` avec `save_viewable_note`, `get_viewable_note`, `find_notes_for_viewing_key`, `save_note_merkle_root` et `get_note_merkle_root`, compatibilité des helpers/tests `Account` mise à jour dans `src/blockchain/validation.rs` ; tests dédiés validés `viewable_note_matches_its_viewing_key`, `viewable_note_rejects_zero_viewing_key`, `storage_saves_and_scans_viewable_notes_by_viewing_key`, `storage_persists_note_merkle_root_globally_and_by_shard` ; `cargo test privacy:: --lib` OK (`27 passed; 0 failed`), `cargo test storage:: --lib` OK (`11 passed; 0 failed`) et `CARGO_INCREMENTAL=0 RUSTFLAGS="-C debuginfo=0" cargo test --lib` OK (`141 passed; 0 failed`)
- [x] 7.10 : conversion entre modèle public et modèle anonyme implémentée avec ajout de `TransactionType::{PublicToAnon, AnonToPublic}` dans `src/blockchain/types.rs`, nouveau sous-module `src/privacy/conversion.rs` exporté via `src/privacy/mod.rs`, structures `PublicToAnonRequest`, `AnonToPublicRequest`, `PublicToAnonConversion`, `AnonToPublicConversion` et erreur `ConversionError`, plus helpers `convert_public_to_anon` et `convert_anon_to_public` ; la conversion `PublicToAnon` débite le solde public, crée une `ViewableNote`, ajoute l’engagement dans `anonymous_state.note_commitments` et recalcule la racine Merkle ; la conversion `AnonToPublic` vérifie la possession via `derive_note_recipient_public_key`, valide la preuve de Merkle, retire l’engagement consommé et crédite le solde public ; tests dédiés validés avec `cargo test privacy::conversion:: --lib` (`5 passed; 0 failed`), régression confidentialité validée avec `cargo test privacy:: --lib` (`32 passed; 0 failed`) et régression bibliothèque complète validée avec `CARGO_INCREMENTAL=0 RUSTFLAGS="-C debuginfo=0" cargo test --lib` (`146 passed; 0 failed`)
- [x] 7.11 : test d’intégration ajouté dans `src/privacy/mod.rs` avec `anon_transaction_public_inputs_do_not_reveal_which_same_value_note_was_spent`, validant qu’on peut construire deux transactions anonymes à partir de deux notes d’entrée différentes de même valeur, obtenir des preuves distinctes, vérifier chaque preuve correctement, conserver les mêmes entrées publiques visibles (`merkle_root`, `fee`, `output_commitments`) et ne pas révéler publiquement quelle note a été dépensée ; validations réussies avec `cargo test anon_transaction_public_inputs_do_not_reveal_which_same_value_note_was_spent --lib` (`1 passed; 0 failed`), `cargo test privacy:: --lib` (`33 passed; 0 failed`) et `CARGO_INCREMENTAL=0 RUSTFLAGS="-C debuginfo=0" cargo test --lib` (`147 passed; 0 failed`)
- [x] 7.12 : benchmark de performance des preuves anonymes validé avec ajout du benchmark Criterion `benches/privacy_benchmark.rs`, déclaration `[[bench]]` correspondante dans `Cargo.toml` et remplacement du test de performance debug par le test fonctionnel `zk_transfer_proof_generation_and_verification_succeeds` dans `src/privacy/mod.rs` ; mesures observées en build optimisé via `cargo bench` : génération de preuve `privacy_zk_transfer_proof_generation` ≈ `1.5581–1.5642 s` (< `10 s`) et vérification `privacy_zk_transfer_proof_verification` ≈ `1.1821–1.1865 ms` (< `100 ms`) ; validations réussies avec `cargo bench --bench privacy_benchmark --no-run`, `LC_ALL=C cargo bench --bench privacy_benchmark`, `cargo test zk_transfer_proof_generation_and_verification_succeeds --lib`, `cargo test privacy:: --lib` (`34 passed; 0 failed`) et `CARGO_INCREMENTAL=0 RUSTFLAGS="-C debuginfo=0" cargo test --lib` (`148 passed; 0 failed`)
- [x] 7.13 : sécurité confidentialité renforcée avec deux nouveaux tests de défense dans `src/privacy/mod.rs` et `src/privacy/conversion.rs` : `anon_transaction_rejects_truncated_serialized_proof` rejette une preuve Groth16 sérialisée tronquée/falsifiée, et `anon_to_public_rejects_spending_same_note_twice_after_state_update` vérifie qu’une même note ne peut pas être reconvertie une seconde fois après consommation via `anonymous_state.note_commitments` ; validations réussies avec `cargo test anon_transaction_rejects_truncated_serialized_proof --lib` (`1 passed; 0 failed`), `cargo test anon_to_public_rejects_spending_same_note_twice_after_state_update --lib` (`1 passed; 0 failed`), `cargo test privacy:: --lib` (`36 passed; 0 failed`) et `CARGO_INCREMENTAL=0 RUSTFLAGS="-C debuginfo=0" cargo test --lib` (`150 passed; 0 failed`)
- [x] 8.1 : dépendance `jsonrpsee` ajoutée dans `Cargo.toml` en version `0.26.0` avec `default-features = false` et `features = ["server"]`, `Cargo.lock` mis à jour avec les crates serveur associées, et validation de compilation réalisée via `cargo check --lib` ; arbre de dépendances confirmé avec `cargo tree -p jsonrpsee` ; incident disque non bloquant rencontré pendant la première compilation (`librocksdb-sys` en erreur `No space left on device`) puis résolu proprement par `cargo clean`, qui a supprimé `12743` fichiers et libéré `8.3 GiB` avant relance réussie ; validations finales réussies avec `cargo check --lib` (`Finished `dev` profile [unoptimized + debuginfo] target(s) in 1m 20s`) et `cargo tree -p jsonrpsee`.
- [x] 8.2 : serveur RPC JSON-RPC de base implémenté avec `jsonrpsee` dans `src/api/mod.rs` et branché dans `src/main.rs` avec un port RPC séparé du P2P (`PRIMS_RPC_ADDRESS`, par défaut `127.0.0.1:7002`) et un chemin RocksDB configurable (`PRIMS_DB_PATH`, par défaut `./data/prims-rocksdb`) ; méthodes RPC ajoutées : `get_block(height/hash)`, `get_transaction(hash)`, `send_transaction(hex_tx)`, `get_balance(address)`, `get_info()`, `get_validators()` et `get_note_commitments()` ; dépendance `hex = "0.4"` ajoutée dans `Cargo.toml` pour les entrées/sorties hexadécimales ; `Mempool` étendu avec `len_async()` pour corriger un panic Tokio causé par un appel synchrone `block_on` depuis le runtime RPC ; validations réussies avec `cargo check --lib`, `cargo check`, démarrage réel via `cargo run --bin prims` avec P2P sur `7001` et RPC sur `7002`, appels `curl` validés pour `get_info`, `get_validators`, `get_note_commitments`, `get_balance`, `get_block`, `get_transaction` et chemin d’erreur contrôlé de `send_transaction`, puis test d’intégration `cargo test --test rpc_api -- --nocapture` validant le chemin de succès de `send_transaction` (`1 passed; 0 failed`).
- [x] 8.3 : rate limiting RPC ajouté dans `src/api/mod.rs` via `governor = "0.10"` avec `RpcState` étendu d’un `DefaultDirectRateLimiter` global, quota par défaut `20` requêtes/s (`DEFAULT_RPC_RATE_LIMIT_PER_SECOND`), helper `RpcState::with_rate_limit(...)` pour injecter un quota personnalisé en test, contrôle centralisé `check_rate_limit()` appliqué à toutes les méthodes RPC (`get_block`, `get_transaction`, `send_transaction`, `get_balance`, `get_info`, `get_validators`, `get_note_commitments`) et erreur JSON-RPC dédiée `-32029` / `rate limit exceeded` en cas de dépassement ; validations réussies avec `cargo check --lib`, `cargo check` et `cargo test --test rpc_api -- --nocapture`, désormais à `2 passed; 0 failed` grâce au nouveau test `get_validators_is_rate_limited_after_quota_is_exhausted` en plus du test de succès `send_transaction_accepts_valid_bincode_hex_transaction`.

- [x] 8.4 : explorateur web minimal implémenté dans `src/bin/prims-explorer.rs` avec `axum`, templates `askama` inline et client HTTP `reqwest`, binaire séparé branché sur le serveur RPC existant via `PRIMS_RPC_URL` (par défaut `http://127.0.0.1:7002`) et exposé via `PRIMS_EXPLORER_ADDRESS` (par défaut `127.0.0.1:7003`) ; page d’accueil validée avec sections `Recherche de solde`, `Informations du nœud`, `Validateurs` et `Commitments anonymes`, route `/address?address=...` validée pour afficher l’adresse demandée et la réponse RPC `get_balance` ; validations réussies avec `cargo check --bin prims-explorer`, démarrage réel du nœud via `cargo run --bin prims` (RPC `7002`), démarrage réel de l’explorateur via `cargo run --bin prims-explorer` (HTTP `7003`), `curl -I http://127.0.0.1:7003` retournant `HTTP/1.1 200 OK`, et vérifications HTML des pages `/` et `/address`.
- [x] 8.5 : `src/bin/prims-cli.rs` a été remplacé par un CLI utilisateur branché sur le RPC JSON-RPC existant via `reqwest`, avec les commandes `generate-key`, `balance <address>`, `send <to> <amount> [--anon]`, `stake <amount>`, `unstake`, `list-validators`, `create-contract <wasm_file>` et `call-contract <address> <method> <params>` ; le CLI utilise `PRIMS_RPC_URL` (défaut `http://127.0.0.1:7002`), génère des paires Ed25519 en JSON, charge la clé privée depuis `PRIMS_SECRET_KEY_HEX` ou `PRIMS_SECRET_KEY_FILE`, sérialise les transactions en `bincode` hexadécimal puis appelle `send_transaction` ; les commandes contrat sont présentes mais renvoient explicitement un statut non supporté tant que le backend contrat/RPC n’existe pas encore ; validations réussies avec `cargo check --bin prims-cli`, `cargo run --bin prims-cli -- --help`, `cargo run --bin prims-cli -- list-validators` (sortie `[]`), `cargo run --bin prims-cli -- balance <adresse_publique>` (réponse JSON cohérente), `cargo run --bin prims-cli -- send <adresse_publique> 1`, `cargo run --bin prims-cli -- stake 10` et `cargo run --bin prims-cli -- unstake` (erreurs RPC métier cohérentes de solde insuffisant), ainsi que `cargo run --bin prims-cli -- create-contract ~/Documents/prims_sources/step-8.5/dummy_contract.wasm` et `cargo run --bin prims-cli -- call-contract <adresse_publique> ping '{}'` (messages explicites “non supportée”) ; le fichier temporaire contenant une clé privée de test a été supprimé après validation.

- [x] 8.6 : stockage sécurisé des clés implémenté dans `src/bin/prims-cli.rs` avec dépendances `age`, `rpassword`, `secrecy` et `zeroize` ajoutées dans `Cargo.toml` ; la commande `generate-key` accepte désormais `--encrypted-file <path>` pour écrire la clé privée dans un fichier chiffré Age avec mot de passe interactif et permissions `600`, `PRIMS_SECRET_KEY_FILE` accepte maintenant un fichier chiffré avec déchiffrement interactif par mot de passe tout en conservant la compatibilité avec les formats JSON/hex existants, et les buffers sensibles sont effacés via `zeroize` ; correctif appliqué pour éviter une double demande de mot de passe dans `stake`/`unstake` ; validations réussies avec `cargo check --bin prims-cli`, `cargo run --bin prims-cli -- generate-key --help`, génération d’un portefeuille chiffré de test (`test-wallet.age`) sans exposition de `secret_key`, lecture réussie via `PRIMS_SECRET_KEY_FILE=... cargo run --bin prims-cli -- unstake` avec une seule invite de mot de passe puis erreur RPC métier cohérente de solde insuffisant, et rejet correct d’un mauvais mot de passe avec `impossible de déchiffrer ...`.

- [x] 8.7 : documentation Markdown de l API JSON-RPC ajoutee dans `docs/api_rpc.md` avec regles generales d usage, ports par defaut (`7001` P2P, `7002` RPC, `7003` explorateur), methodes `get_info`, `get_validators`, `get_note_commitments`, `get_balance`, `get_block`, `get_transaction` et `send_transaction`, exemples `curl`, validations metier et gestion d erreurs (`-32029` / `rate limit exceeded`) ; `README.md` a ete enrichi d une section `Documentation` pointant vers `docs/api_rpc.md` et `docs/privacy_trusted_setup.md` ; validations finales reussies par inspection structuree de `docs/api_rpc.md` et du `README.md`.

- [x] 8.8 : tests d’intégration RPC + CLI validés avec ajout de `tests/cli_integration.rs` couvrant `balance`, `list-validators`, `send`, `stake` et `unstake` via le vrai binaire `prims-cli` connecté à un serveur JSON-RPC de test `jsonrpsee` ; la suite existante `tests/rpc_api.rs` reste valide pour `send_transaction` et le rate limiting ; validations réussies avec `cargo test --test cli_integration -- --nocapture` (`5 passed`) puis `cargo test --test rpc_api --test cli_integration -- --nocapture` (`rpc_api: 2 passed`, `cli_integration: 5 passed`).

- [x] 8.9 : audit de securite de l API publique realise ; `get_balance` n expose plus `viewing_hint` dans `src/api/mod.rs` ; `docs/api_rpc.md` documente desormais explicitement que `viewing_hint` est volontairement omis ; un test de non-regression `get_balance_omits_sensitive_viewing_hint_metadata` a ete ajoute dans `tests/rpc_api.rs` ; validations reussies avec `cargo fmt --check`, `cargo test --test rpc_api -- --nocapture` (`3 passed`) et `cargo test --test rpc_api --test cli_integration -- --nocapture` (`rpc_api: 3 passed`, `cli_integration: 5 passed`).
- [x] 9.1 : dependances de la machine virtuelle WASM ajoutees dans `Cargo.toml` avec `wasmtime = "42.0.1"` et `wasmtime-wasi = "42.0.1"` ; `Cargo.lock` a ete mis a jour en consequence ; validation reussie avec `cargo check` sur macOS sans regression de compilation.

- [x] 9.2 : module `src/vm/mod.rs` cree avec une structure minimale `WasmVM` (`#[derive(Debug, Default)]`) et un constructeur `WasmVM::new()` ; `src/lib.rs` exporte desormais `pub mod vm;` ; validations reussies avec inspection des fichiers puis `cargo check` sur macOS sans regression de compilation.

- [x] 9.3 : interface des host functions WASM definie dans `src/vm/mod.rs` avec `WasmExecutionContext`, `ContractEvent` et les fonctions `get_balance`, `transfer`, `get_caller`, `get_block_height`, `set_storage`, `get_storage` et `emit_event` ; `transfer` prepare pour l instant une transaction publique en file interne `pending_transfers`, `set_storage`/`get_storage` utilisent l espace de noms `contract_storage_key`, et `emit_event` enregistre les evenements en memoire ; tests unitaires ajoutes pour le solde, le transfert, le storage et les evenements ; validations reussies avec `cargo fmt --all --check`, `cargo check` et `cargo test vm:: -- --nocapture` (`6 passed; 0 failed`).

- [x] 9.4 : implémentation Rust des host functions WASM renforcée dans `src/vm/mod.rs` avec helpers `require_contract_address` et `require_contract_account` ; `transfer`, `set_storage` et `get_storage` vérifient désormais qu’un compte contrat réel existe et que son `code_hash` est présent avant d’autoriser les opérations sensibles ; le namespace `contract_storage_key` est conservé pour le storage du contrat, `emit_event` réutilise la validation d’adresse de contrat, et un test de non-régression `set_storage_rejects_non_contract_accounts` a été ajouté ; validations réussies avec `cargo fmt --all --check`, `cargo check`, `cargo test vm:: -- --nocapture` (`7 passed; 0 failed`) et `cargo test set_storage_rejects_non_contract_accounts -- --nocapture` (`1 passed; 0 failed`).

- [x] 9.5 : stockage persistant des contrats implémenté avec la structure `Contract { code_wasm, storage_root }` dans `src/blockchain/types.rs`, clé `c:{address}` normalisée via `contract_key` dans `src/storage/keys.rs`, helpers `update_contract`, `get_contract`, `set_contract_storage`, `get_contract_storage` et recalcul déterministe de `storage_root` par hash SHA-256 des slots triés dans `src/storage/mod.rs` ; `WasmVM::set_storage` et `WasmVM::get_storage` passent désormais par la table `contracts` tout en conservant le namespace `m:{address}:{key}` pour les slots ; tests ajoutés `contract_structure_roundtrip_with_bincode` et `contract_storage_roundtrip_updates_storage_root`, plus adaptation du test VM `set_storage_and_get_storage_roundtrip_under_contract_namespace` ; validations réussies avec `cargo fmt --all`, `cargo check`, `cargo test contract_storage_roundtrip_updates_storage_root -- --nocapture` (`1 passed; 0 failed`), `cargo test set_storage_and_get_storage_roundtrip_under_contract_namespace -- --nocapture` (`1 passed; 0 failed`), `cargo test vm:: -- --nocapture` (`7 passed; 0 failed`) et `cargo test storage:: -- --nocapture` (`12 passed; 0 failed`).

- [x] 9.6 : moteur de gaz Wasm implémenté dans `src/vm/mod.rs` avec `DEFAULT_WASM_FUEL_LIMIT = 100_000`, structure `WasmGasConfig`, stockage de la configuration dans `WasmVM`, activation de `Config::consume_fuel(true)` côté `wasmtime`, création d’un `Store` instrumenté via `new_metered_store`, alimentation initiale en fuel via `set_fuel`, lecture du fuel restant via `remaining_fuel`, et validation de la limite via `set_fuel_limit` qui refuse `0` ; tests ajoutés `set_fuel_limit_rejects_zero`, `new_metered_store_starts_with_configured_fuel_limit`, `wasm_execution_consumes_fuel` et `wasm_execution_traps_when_fuel_is_exhausted` ; validations réussies avec `cargo fmt --all`, `cargo check`, `cargo test wasm_execution_ -- --nocapture` (`2 passed; 0 failed`), `cargo test vm:: -- --nocapture` (`11 passed; 0 failed`) et `cargo test --lib -- --nocapture` (`163 passed; 0 failed`).

- [x] 9.7 : transaction native `DeployContract` ajoutée avec `TransactionType::DeployContract`, calcul d’adresse déterministe via `derive_contract_address(sender, code_wasm)` et hash de code via `contract_code_hash` dans `src/blockchain/hash.rs`, validation RPC minimale du déploiement dans `src/api/mod.rs`, helper `RocksDbStorage::deploy_contract(...)` dans `src/storage/mod.rs` pour persister le code Wasm et créer le compte contrat avec `code_hash`, et commande CLI `create-contract <wasm_file>` désormais branchée sur une vraie soumission RPC signée ; tests ajoutés `derive_contract_address_is_deterministic_for_same_sender_and_code`, `derive_contract_address_changes_when_sender_or_code_changes`, `deploy_contract_persists_code_and_contract_account`, `deploy_contract_rejects_empty_bytecode`, `deploy_contract_rejects_same_sender_and_code_twice` et `create_contract_command_submits_deploy_contract_transaction` ; validations réussies avec `cargo fmt --all`, `cargo check`, `cargo test deploy_contract_ -- --nocapture` (`3 passed; 0 failed`), `cargo test create_contract_command_submits_deploy_contract_transaction -- --nocapture` (`1 passed; 0 failed`), `cargo test --test cli_integration -- --nocapture` (`6 passed; 0 failed`) et `cargo test --lib -- --nocapture` (`168 passed; 0 failed`).

- [x] 9.8 : transaction native `CallContract` ajoutée avec `TransactionType::CallContract` et `ContractCallPayload { method, params, gas_limit }` dans `src/blockchain/types.rs` ; `src/api/mod.rs` valide désormais les appels de contrat (`fee` fixe, `amount = 0`, adresse cible non vide, payload sérialisé obligatoire, `method` non vide et `gas_limit > 0`) ; `src/bin/prims-cli.rs` branche maintenant réellement `call-contract <address> <method> <params> [--gas-limit <u64>]` sur une soumission RPC signée avec sérialisation JSON des paramètres et `gas_limit` par défaut `100000` ; tests ajoutés `call_contract_transaction_roundtrip_with_bincode`, `send_transaction_accepts_valid_call_contract_transaction`, `send_transaction_rejects_call_contract_with_zero_gas_limit` et `call_contract_command_submits_call_contract_transaction` ; validations réussies avec `cargo fmt --all`, `cargo check`, `cargo test --test rpc_api -- --nocapture` (`5 passed; 0 failed`), `cargo test --test cli_integration -- --nocapture` (`7 passed; 0 failed`), `cargo test call_contract_ -- --nocapture` (`4 passed; 0 failed`) et `cargo test --lib -- --nocapture` (`169 passed; 0 failed`).

- [x] 9.9 : exécution réelle des contrats Wasm branchée avec `WasmVM::execute_contract_call` dans `src/vm/mod.rs`, chargement du code contrat depuis RocksDB, instanciation Wasmtime, liaison des host functions (`get_block_height`, `get_caller_len`, `copy_caller`, `get_params_len`, `copy_params`, `get_balance`, `set_storage`, `get_storage_len`, `copy_storage`, `emit_event`, `transfer`), appel d’une fonction exportée par son nom, consommation de fuel, récupération des transferts/événements émis et mise à jour réelle du storage contrat ; `src/api/mod.rs` exécute désormais réellement `CallContract` pendant la validation RPC via `WasmExecutionContext` ; `tests/rpc_api.rs` et `tests/cli_integration.rs` préchargent maintenant un vrai contrat Wasm de test et vérifient la mise à jour du slot `counter` ; validations réussies avec `cargo fmt --all --check`, `cargo check`, `cargo test --lib -- --nocapture` (`170 passed; 0 failed`), `cargo test --test rpc_api -- --nocapture` (`5 passed; 0 failed`), `cargo test --test cli_integration -- --nocapture` (`7 passed; 0 failed`) et `cargo test -- --nocapture` (suite complète verte).

- [x] 9.10 : sécurité d’exécution Wasm renforcée avec écriture du storage contrat désormais tamponnée en mémoire dans `src/vm/mod.rs` via `staged_storage_writes`, commit final uniquement après succès de `WasmVM::execute_contract_call`, lecture prioritaire des écritures tamponnées pendant l’exécution, et ajout dans `src/storage/mod.rs` de `commit_contract_storage_batch(...)` basé sur `rocksdb::WriteBatch` pour appliquer atomiquement les slots modifiés et recalculer `storage_root` ; nouveaux tests ajoutés pour prouver le rollback sans écriture partielle sur trap Wasm et dépassement de gaz dans `src/vm/mod.rs`, rejet RPC avec storage intact dans `tests/rpc_api.rs`, et remontée CLI correcte de l’erreur avec rollback vérifié dans `tests/cli_integration.rs` ; validations réussies avec `cargo fmt --all --check`, `cargo test --lib -- --nocapture` (`172 passed; 0 failed`), `cargo test --test rpc_api -- --nocapture` (`6 passed; 0 failed`), `cargo test --test cli_integration -- --nocapture` (`8 passed; 0 failed`) et `cargo test -- --nocapture` (suite complète verte).

- [x] 9.11 : tests fonctionnels d’un contrat Wasm simple de type jeton/ERC20 minimal ajoutés dans `tests/cli_integration.rs` avec `create_contract_then_call_token_contract_updates_caller_balance_storage` ; le scénario déploie le contrat via `create-contract`, matérialise le déploiement en storage de test via `RocksDbStorage::deploy_contract(...)`, avance le nonce du déployeur pour simuler l’inclusion, appelle ensuite `call-contract <adresse> mint 1000 --gas-limit 50000`, puis vérifie que le storage du contrat contient bien le solde `1000` sous la clé du caller ; validations réussies avec `cargo test --test cli_integration create_contract_then_call_token_contract_updates_caller_balance_storage -- --nocapture` (`1 passed; 0 failed`), `cargo test --test cli_integration -- --nocapture` (`9 passed; 0 failed`), `cargo fmt --all --check` et `cargo test -- --nocapture` (suite complète verte : `172` tests lib, `9` tests CLI, `6` tests RPC, aucun échec).

- [x] 9.12 : benchmark Criterion ajouté avec `benches/wasm_benchmark.rs` et l’entrée `[[bench]] name = "wasm_benchmark"` dans `Cargo.toml` ; le benchmark `wasm_execute_simple_contract_call` mesure l’exécution d’un contrat Wasm simple exportant `increment` via `WasmVM::execute_contract_call(...)` sur un storage RocksDB de test ; validations réussies avec `cargo fmt --all --check`, `cargo bench --bench wasm_benchmark --no-run` et `cargo bench --bench wasm_benchmark -- --noplot` ; mesure observée : `time: [200.43 µs 200.91 µs 201.48 µs]`, soit environ `0.201 ms`, très en dessous de l’objectif roadmap `< 10 ms`.

- [x] 9.13 : sécurité Wasm renforcée avec validation ciblée des contrats malveillants ; le runtime stoppe correctement une boucle infinie via le fuel (`wasm_execution_traps_when_fuel_is_exhausted`), annule bien les écritures de storage quand le fuel est épuisé pendant `execute_contract_call` (`execute_contract_call_rolls_back_storage_when_fuel_is_exhausted`), et rejette désormais explicitement un accès mémoire hors limites avec rollback atomique grâce au nouveau test `execute_contract_call_rolls_back_storage_on_out_of_bounds_memory_access` ajouté dans `src/vm/mod.rs` ; validations réussies avec `cargo fmt --all`, `cargo fmt --all --check`, `cargo test wasm_execution_traps_when_fuel_is_exhausted -- --nocapture`, `cargo test execute_contract_call_rolls_back_storage_when_fuel_is_exhausted -- --nocapture` et `cargo test execute_contract_call_rolls_back_storage_on_out_of_bounds_memory_access -- --nocapture` (trois tests ciblés verts, `1 passed; 0 failed` chacun).

- [x] 10.1 : workflow GitHub Actions multi-OS ajouté dans `.github/workflows/build.yml` pour automatiser les builds sur `ubuntu-latest`, `macos-latest` et `windows-latest`, avec déclencheurs `push`, `pull_request` et `workflow_dispatch` sur `main`, installation de Rust stable via `dtolnay/rust-toolchain@stable`, cache de build via `Swatinem/rust-cache@v2`, et compilation `cargo build --locked --all-targets` ; validations locales réussies avec inspection du workflow, vérification de la matrice des 3 OS et validation syntaxique YAML par Ruby (`YAML OK: .github/workflows/build.yml`) ; un premier run GitHub a révélé un décalage du benchmark `benches/storage_benchmark.rs` avec les structures `Transaction` / `Block` actuelles ainsi qu’une mise à jour nécessaire de `rustls-webpki` dans `Cargo.lock`, puis les correctifs ont été poussés ; validation réelle obtenue sur GitHub Actions avec succès des trois jobs `Build (macos-latest)`, `Build (windows-latest)` et `Build (ubuntu-latest)` ; la CI GitHub a ensuite été entièrement remise au vert avec succès du workflow `Security Audit`.

- [x] 10.2 : site web testnet simple finalisé dans `src/bin/prims-explorer.rs` avec page d accueil `Prims Testnet`, section instructions, recherche de solde, route `POST /faucet`, activation conditionnelle du faucet via `PRIMS_SECRET_KEY_FILE` ou `PRIMS_SECRET_KEY_HEX`, paramètres `PRIMS_FAUCET_AMOUNT` / `PRIMS_FAUCET_SOURCE_SHARD` / `PRIMS_FAUCET_DESTINATION_SHARD`, documentation ajoutée au `README.md`, validations réussies avec `cargo fmt`, `cargo fmt --check`, `cargo build --bin prims-explorer`, démarrage HTTP sur `127.0.0.1:7003`, vérification de la page d accueil et refus sécurisé du faucet sans clé.

- [x] 10.3 : seed node public finalisé avec lecture de `PRIMS_EXTERNAL_ADDRESS` dans `src/network/config.rs`, annonce de l adresse publique via `swarm.add_external_address(...)` dans `src/network/node.rs`, prise en charge d une identité réseau persistante via `PRIMS_NETWORK_SECRET_KEY_FILE` / `PRIMS_NETWORK_SECRET_KEY_HEX`, et affichage `External address` au démarrage dans `src/main.rs` ; validations locales réussies avec `cargo fmt`, `cargo fmt --check`, `cargo build --bin prims`, réponse RPC correcte sur `http://127.0.0.1:7002` et redémarrage confirmant un `PeerId` stable grâce à une clé locale hors dépôt.

- [x] 10.4 : programme de bug bounty testnet documenté dans `docs/bug_bounty.md` avec objectif, canal de signalement, périmètre, hors périmètre, règles de test, catégories de sévérité (`Critique`, `Haute`, `Moyenne`, `Faible`) et récompenses proposées en `PRIMS` ; `README.md` enrichi dans la section `Documentation` avec le lien `docs/bug_bounty.md` ; revue Git finale effectuée sans secret ni clé sensible ; commit `4156457` (`docs(security): add bug bounty program for step 10.4`) créé puis poussé avec succès sur `origin/main`, vérification confirmée avec `HEAD` local = `origin/main`.

## Prochaine étape
10.5 – Finaliser la clôture Git/GitHub des validations auto-hébergées sur machine unique : vérifier les fichiers modifiés (`docker-compose.yml`, `journal_avancement.md`), faire le commit propre, pousser sur `main`, vérifier le push, puis sauvegarder localement les artefacts finaux de l’étape 10.5.

## Notes importantes
- Étape 10.4 clôturée Git/GitHub : commit `4156457` (`docs(security): add bug bounty program for step 10.4`) créé puis poussé avec succès sur `origin/main`, vérification confirmée avec `HEAD` local = `origin/main`.
- Validation complémentaire 10.5 en mode auto-hébergé sur machine unique : adaptation de `docker-compose.yml` pour supprimer les seeds `/dns4/...` non supportés par le transport actuel, démarrage réussi d'un cluster local `shard1/shard2/shard3` sur un seul hôte avec découverte réseau via `Discovered(...)`, test distant réussi avec `PRIMS_REMOTE_SEED_NODE="/ip4/127.0.0.1/tcp/7001" PRIMS_LOAD_SKIP_BUILD=1 bash scripts/test_transaction_load.sh 7001 1 5 1` (`5` transactions publiées, `0` client en échec), benchmark multi-nœuds distant réussi avec `PRIMS_BENCH_REMOTE_SEEDS="/ip4/127.0.0.1/tcp/7001,/ip4/127.0.0.1/tcp/7002,/ip4/127.0.0.1/tcp/7003" PRIMS_BENCH_SKIP_BUILD=1 PRIMS_LOAD_SKIP_BUILD=1 bash scripts/benchmark_shards_tps.sh 3 1 5 1` (scénarios 1/2/3 nœuds : `5/10/15` transactions publiées, `0` échec), puis arrêt propre du cluster Docker.
- Décision d'orientation prise pour 10.5 : Prims ne dépendra pas de VPS payants ; la suite des tests de charge visera des nœuds auto-hébergés ou des machines réelles, dans un esprit proche des déploiements de nœuds observés sur des réseaux publics ; les outils déjà créés (`flood`, `scripts/test_transaction_load.sh`, `scripts/benchmark_shards_tps.sh`, `scripts/run_vps_node.sh`, `docs/load_testing.md`) sont conservés et réinterprétés comme support de déploiement distant générique, sans obligation de VPS.
- Étape 10.5 démarrée localement : restauration de la sous-commande `flood` dans `src/bin/prims-cli.rs`, adaptation de `scripts/test_transaction_load.sh` pour un mode local ou distant via `PRIMS_REMOTE_SEED_NODE` / `PRIMS_LOAD_SKIP_BUILD`, adaptation de `scripts/benchmark_shards_tps.sh` pour un mode Docker ou distant via `PRIMS_BENCH_REMOTE_SEEDS` / `PRIMS_BENCH_SKIP_BUILD`, et ajout de la documentation `docs/load_testing.md` référencée depuis `README.md` ; validations locales réussies avec `cargo fmt --all --check`, `cargo build --bin prims --bin prims-cli`, `bash -n scripts/test_transaction_load.sh scripts/benchmark_shards_tps.sh`, smoke test local `bash scripts/test_transaction_load.sh 7001 1 5 1`, benchmark Docker minimal `PRIMS_BENCH_SKIP_BUILD=1 bash scripts/benchmark_shards_tps.sh 1 1 5 1`, benchmark distant simulé via `PRIMS_BENCH_REMOTE_SEEDS` sur un seed node local dédié, bundle local `.tmp/step-10.5/prims_vps_bundle.tgz` généré avec SHA-256 `2a6b3321b4d12a1c06f72341ceb7ac350e98c0e183248c79b904c29ac61adc28`, puis réorientation documentaire vers des machines réelles / auto-hébergées ; étape encore à clôturer totalement côté Git/GitHub pour la dernière validation Docker auto-hébergée.
- Sauvegardes locales créées pendant l’étape 10.4 :
  - `~/Documents/prims_sources/step-10.4/README.md.before_step_10_4`
- Étape 10.3 clôturée Git/GitHub : commit `f4dd47c` (`feat(network): finalize public seed node for step 10.3`) créé puis poussé avec succès sur `origin/main`, vérification confirmée avec `HEAD` local = `origin/main`, et sauvegardes finales créées dans `~/Documents/prims_sources/step-10.3/`.
- Étape 10.4 clôturée Git/GitHub : `docs/bug_bounty.md` ajouté pour documenter le programme de bug bounty testnet (objectif, canal de signalement, périmètre, hors périmètre, règles de test, catégories de sévérité `Critique` / `Haute` / `Moyenne` / `Faible` et récompenses proposées en `PRIMS`), `README.md` mis à jour dans la section `Documentation`, puis commit `4156457` (`docs(security): add bug bounty program for step 10.4`) poussé avec succès sur `origin/main` ; vérification confirmée avec `HEAD` local = `origin/main`, sans secret ni clé sensible dans les fichiers validés.
- Adresse seed node publique observée le 27 mars 2026 : `/ip4/81.65.115.118/tcp/7001/p2p/12D3KooWHAyUEv4HXap4rF7FLmdHJUv9VZ3grWspF95fAtfcw4P1`.
- Remarque sécurité 10.3 : ne jamais commiter, afficher ni partager le contenu de `~/Documents/prims_secrets/prims_network_identity.hex` ; conserver ce fichier local hors dépôt avec permissions `600`.
- Sauvegardes locales créées pendant l’étape 10.3 :
  - `~/Documents/prims_sources/step-10.3/config.rs.before_step_10_3`
  - `~/Documents/prims_sources/step-10.3/node.rs.before_step_10_3`
  - `~/Documents/prims_sources/step-10.3/journal_avancement.md.after_step_10_3`
  - `~/Documents/prims_sources/step-10.3/main.rs.after_step_10_3`
  - `~/Documents/prims_sources/step-10.3/config.rs.after_step_10_3`
  - `~/Documents/prims_sources/step-10.3/node.rs.after_step_10_3`
  - `~/Documents/prims_sources/step-10.3/git_commit_step_10_3.txt`
- Étape 10.2 validée : le site web testnet repose sur le binaire existant `prims-explorer`, enrichi en mini portail `Prims Testnet` avec informations du nœud, validateurs, commitments anonymes, recherche de solde, instructions de démarrage et faucet web ; sans secret configuré, le faucet reste explicitement désactivé et refuse proprement les demandes.
- Remarque sécurité 10.2 : ne jamais commiter ni afficher une vraie clé privée faucet ; utiliser de préférence `PRIMS_SECRET_KEY_FILE` pointant vers un fichier local hors dépôt, ou à défaut `PRIMS_SECRET_KEY_HEX` uniquement pour des clés de test jetables.
- Sauvegardes locales créées pendant l étape 10.2 :
  - `~/Documents/prims_sources/step-10.2/prims-explorer.rs.before_step_10_2`
  - `~/Documents/prims_sources/step-10.2/README.md.before_step_10_2`
  - `~/Documents/prims_sources/step-10.2/journal_avancement.md.before_step_10_2`
  - `~/Documents/prims_sources/step-10.2/prims-explorer.rs.after_step_10_2`
  - `~/Documents/prims_sources/step-10.2/README.md.after_step_10_2`
  - `~/Documents/prims_sources/step-10.2/journal_avancement.md.after_step_10_2`
- Sauvegardes locales créées pendant l’étape 10.1 :
  - `~/Documents/prims_sources/step-10.1/.gitignore.before_first_commit`
  - `~/Documents/prims_sources/step-10.1/Cargo.lock.before_rustls_webpki_update`
  - `~/Documents/prims_sources/step-10.1/storage_benchmark.rs.before_fix_step_10_1`
  - `~/Documents/prims_sources/step-10.1/audit.yml.before_ignore_rustsec_2025_0055`
- Étape 10.1 validée : ajout de `.github/workflows/build.yml` pour builder automatiquement Prims sur Linux, macOS et Windows avec une matrice GitHub Actions ; le workflow utilise `actions/checkout`, `dtolnay/rust-toolchain@stable`, `Swatinem/rust-cache@v2` et exécute `cargo build --locked --all-targets` sur `ubuntu-latest`, `macos-latest` et `windows-latest` ; un premier run GitHub a mis en évidence deux points à corriger : `benches/storage_benchmark.rs` ne suivait plus les champs `tx_type`, `source_shard`, `destination_shard` et `receipts`, et `Cargo.lock` devait être mis à jour pour `rustls-webpki 0.103.10` ; après correction, commit `b2b883d` et push sur `main`, les trois jobs GitHub de build sont passés au vert.
- Remarque 10.1 : le push initial `b5a3906` a envoyé l’ensemble du projet déjà présent localement, pas seulement 10.1 ; les commits suivants `b2b883d` et `33263af` n’ont poussé que les correctifs incrémentaux ; le workflow séparé `Security Audit` a été remis au vert avec `cargo audit --ignore RUSTSEC-2025-0055`, car `ark-relations 0.5.1` impose encore `tracing-subscriber ^0.2` et aucune mise à jour amont corrigée n’est disponible à ce stade ; les avertissements non bloquants `bincode`, `derivative` et `paste` restent à surveiller.
- Sauvegardes locales créées avant l’étape 9.13 :
  - `~/Documents/prims_sources/step-9.13/vm.mod.rs.before_step_9_13`
  - `~/Documents/prims_sources/step-9.13/rpc_api.rs.before_step_9_13`
  - `~/Documents/prims_sources/step-9.13/cli_integration.rs.before_step_9_13`
- Étape 9.13 validée : `src/vm/mod.rs` couvre maintenant explicitement les deux scénarios de sécurité visés par la roadmap pour les contrats Wasm malveillants : une boucle infinie est stoppée par le fuel natif de `wasmtime`, et un accès mémoire hors limites déclenche une erreur `memory access is out of bounds` sans persister d’écriture partielle grâce au rollback atomique déjà en place ; le nouveau test `execute_contract_call_rolls_back_storage_on_out_of_bounds_memory_access` a été ajouté dans `src/vm/mod.rs` et vérifie qu’un contrat qui écrit d’abord `counter=1` puis tente une lecture/écriture hors limites est interrompu et laisse le storage inchangé ; validations réussies avec `cargo fmt --all`, `cargo fmt --all --check`, `cargo test wasm_execution_traps_when_fuel_is_exhausted -- --nocapture`, `cargo test execute_contract_call_rolls_back_storage_when_fuel_is_exhausted -- --nocapture` et `cargo test execute_contract_call_rolls_back_storage_on_out_of_bounds_memory_access -- --nocapture` (trois tests ciblés verts, `1 passed; 0 failed` chacun).
- Remarque 9.13 : la première version du nouveau test mémoire échouait non pas parce que le runtime laissait passer l’attaque, mais parce que l’assertion lisait seulement `error.to_string()` ; le message `memory access is out of bounds` était présent dans la chaîne des causes Wasmtime. Le test a été corrigé pour inspecter `error.chain()`, puis il est passé correctement.
- Remarque portée 9.13 : cette validation confirme bien que le runtime stoppe les scénarios malveillants minimaux demandés par la roadmap (boucle infinie et accès mémoire hors limites), avec rollback du storage contrat ; elle ne constitue pas encore un audit exhaustif de tous les patterns d’attaque Wasm possibles.
- Sauvegardes locales créées avant l’étape 9.12 :
  - `~/Documents/prims_sources/step-9.12/Cargo.toml.before_step_9_12`
  - `~/Documents/prims_sources/step-9.12/cli_integration.rs.before_step_9_12`
  - `~/Documents/prims_sources/step-9.12/vm.mod.rs.before_step_9_12`
- Étape 9.12 validée : un benchmark Criterion dédié `benches/wasm_benchmark.rs` a été ajouté pour mesurer le coût d’exécution d’un contrat Wasm simple ; l’entrée `[[bench]] name = "wasm_benchmark"` a été ajoutée dans `Cargo.toml` ; le scénario prépare un contrat minimal exportant `increment`, l’exécute via `WasmVM::execute_contract_call(...)` sur un storage RocksDB de test, puis mesure le temps d’exécution en build optimisé ; validations réussies avec `cargo fmt --all --check`, `cargo bench --bench wasm_benchmark --no-run` et `cargo bench --bench wasm_benchmark -- --noplot` ; résultat observé : `time: [200.43 µs 200.91 µs 201.48 µs]`, soit environ `0.201 ms`, très en dessous de l’objectif roadmap `< 10 ms`.
- Remarque 9.12 : la première compilation du benchmark a remonté un warning non bloquant d’import inutilisé `StorageBackend` dans `benches/wasm_benchmark.rs` ; il a été supprimé avant la mesure finale pour conserver une base propre sans warning.
- Remarque bench 9.12 : la mesure actuelle couvre un contrat Wasm minimal centré sur un simple `set_storage` via l’export `increment` ; elle valide l’objectif de performance de la roadmap pour un cas simple, mais ne reflète pas encore le coût de scénarios plus lourds (plusieurs host functions, lecture de storage, événements, transferts, paramètres plus volumineux).
- Remarque sécurité 9.12 : le benchmark n’utilise que des adresses et clés de test jetables en mémoire ; ne jamais utiliser, afficher, coller ni committer une vraie clé privée, un mot de passe, un token ou le contenu de `~/Documents/prims_sources/step-8.6/test-wallet.age`.
- Sauvegardes locales créées avant l’étape 9.11 :
  - `~/Documents/prims_sources/step-9.11/rpc_api.rs.before_step_9_11`
  - `~/Documents/prims_sources/step-9.11/cli_integration.rs.before_step_9_11`
  - `~/Documents/prims_sources/step-9.11/vm.mod.rs.before_step_9_11`
  - `~/Documents/prims_sources/step-9.11/api.mod.rs.before_step_9_11`
- Étape 9.11 validée : un test d’intégration CLI `create_contract_then_call_token_contract_updates_caller_balance_storage` a été ajouté dans `tests/cli_integration.rs` pour couvrir un contrat Wasm simple de type jeton/ERC20 minimal ; le test exécute `create-contract`, calcule l’adresse déterministe avec `derive_contract_address(...)`, matérialise ensuite le déploiement côté stockage de test avec `RocksDbStorage::deploy_contract(...)`, met à jour le nonce du compte déployeur pour simuler l’inclusion de la transaction de déploiement, appelle `call-contract <adresse> mint 1000 --gas-limit 50000`, puis vérifie que le storage du contrat contient bien `b"1000"` sous la clé du caller ; validations réussies avec `cargo test --test cli_integration create_contract_then_call_token_contract_updates_caller_balance_storage -- --nocapture` (`1 passed; 0 failed`), `cargo test --test cli_integration -- --nocapture` (`9 passed; 0 failed`), `cargo fmt --all --check` et `cargo test -- --nocapture` (suite complète verte).
- Remarque 9.11 : le contrat de test valide bien le scénario fonctionnel demandé par la roadmap (déploiement, appel, vérification d’un solde), mais sous la forme d’un jeton/ERC20 minimal centré sur l’écriture/lecture du storage contrat ; les fonctions complètes de standard ERC20 (`transfer`, `approve`, `allowance`) ne sont pas encore implémentées dans ce prototype.
- Remarque exécution 9.11 : pour rester cohérent avec l’architecture actuelle, le test matérialise explicitement le déploiement dans RocksDB de test avant l’appel du contrat, car `DeployContract` est aujourd’hui validée et acceptée côté RPC/CLI mais pas encore automatiquement incluse/appliquée dans un pipeline de bloc pendant le test d’intégration.
- Remarque temps de test 9.11 : `cargo test -- --nocapture` reste long sur macOS à cause des tests confidentialité Groth16 ; sur cette validation, la suite complète est restée verte avec plusieurs messages temporaires `has been running for over 60 seconds` puis une fin correcte en `670.64 s`, ce qui reste attendu dans le prototype actuel.

- Sauvegardes locales créées avant l’étape 9.10 :
  - `~/Documents/prims_sources/step-9.10/vm.mod.rs.before_step_9_10`
  - `~/Documents/prims_sources/step-9.10/api.mod.rs.before_step_9_10`
  - `~/Documents/prims_sources/step-9.10/storage.mod.rs.before_step_9_10`
  - `~/Documents/prims_sources/step-9.10/rpc_api.rs.before_step_9_10`
  - `~/Documents/prims_sources/step-9.10/cli_integration.rs.before_step_9_10`
- Sauvegardes locales créées après l’étape 9.10 :
  - `~/Documents/prims_sources/step-9.10/vm.mod.rs.after_step_9_10`
  - `~/Documents/prims_sources/step-9.10/storage.mod.rs.after_step_9_10`
  - `~/Documents/prims_sources/step-9.10/rpc_api.rs.after_step_9_10`
  - `~/Documents/prims_sources/step-9.10/cli_integration.rs.after_step_9_10`
- Étape 9.10 validée : l’exécution Wasm des contrats n’écrit plus directement dans RocksDB pendant l’appel ; `src/vm/mod.rs` tamponne désormais les modifications de storage dans `staged_storage_writes`, les relit immédiatement pendant l’exécution pour conserver une vue cohérente, puis appelle `commit_contract_storage_batch(...)` seulement si la fonction exportée se termine sans erreur ; `src/storage/mod.rs` applique alors les écritures via un `rocksdb::WriteBatch`, met à jour `storage_root` dans le même batch atomique et évite ainsi toute persistance partielle en cas de trap Wasm ou d’épuisement de gaz ; `tests/rpc_api.rs` vérifie qu’un `CallContract` qui trap est rejeté côté RPC sans modifier le slot `counter`, et `tests/cli_integration.rs` vérifie qu’un échec d’exécution remonte proprement jusqu’au CLI avec storage intact ; validations réussies avec `cargo fmt --all --check`, `cargo test --lib -- --nocapture` (`172 passed; 0 failed`), `cargo test --test rpc_api -- --nocapture` (`6 passed; 0 failed`), `cargo test --test cli_integration -- --nocapture` (`8 passed; 0 failed`) et `cargo test -- --nocapture` (suite complète verte).
- Remarque 9.10 : les cas de chemin heureux, trap Wasm et dépassement de gaz sont désormais couverts de bout en bout (`VM -> storage -> RPC -> CLI`) avec rollback atomique du storage contrat ; l’étape suivante peut donc se concentrer sur des tests fonctionnels de contrat simple de type ERC20.
- Remarque temps de test 9.10 : `cargo test --lib -- --nocapture` et `cargo test -- --nocapture` restent longs sur macOS à cause des tests confidentialité Groth16, avec plusieurs messages temporaires `has been running for over 60 seconds` ; ce comportement demeure attendu dans le prototype actuel et n’indique pas une régression bloquante.

- Sauvegardes locales créées avant l’étape 9.9 :
  - `~/Documents/prims_sources/step-9.9/vm.mod.rs.before_step_9_9`
  - `~/Documents/prims_sources/step-9.9/storage.mod.rs.before_step_9_9`
  - `~/Documents/prims_sources/step-9.9/api.mod.rs.before_step_9_9`
  - `~/Documents/prims_sources/step-9.9/blockchain.types.rs.before_step_9_9`
- Sauvegardes locales créées après l’étape 9.9 :
  - `~/Documents/prims_sources/step-9.9/vm.mod.rs.after_step_9_9`
  - `~/Documents/prims_sources/step-9.9/api.mod.rs.after_step_9_9`
  - `~/Documents/prims_sources/step-9.9/rpc_api.rs.after_step_9_9`
  - `~/Documents/prims_sources/step-9.9/cli_integration.rs.after_step_9_9`
- Étape 9.9 validée : `src/vm/mod.rs` expose désormais un chemin d’exécution Wasm réel via `WasmVM::execute_contract_call`, avec chargement du bytecode du contrat depuis RocksDB, création d’un `Store` instrumenté en fuel, instanciation du module, liaison des host functions côté runtime (`get_block_height`, `get_caller_len`, `copy_caller`, `get_params_len`, `copy_params`, `get_balance`, `set_storage`, `get_storage_len`, `copy_storage`, `emit_event`, `transfer`) et appel de la fonction exportée par son nom ; `src/api/mod.rs` exécute maintenant réellement `CallContract` au lieu de se limiter à une validation structurelle ; `tests/rpc_api.rs` et `tests/cli_integration.rs` ont été adaptés pour précharger un vrai contrat Wasm de test et vérifier qu’un appel `increment` met bien à jour le slot `counter` dans le storage contrat ; validations réussies avec `cargo fmt --all --check`, `cargo check`, `cargo test --lib -- --nocapture` (`170 passed; 0 failed`), `cargo test --test rpc_api -- --nocapture` (`5 passed; 0 failed`), `cargo test --test cli_integration -- --nocapture` (`7 passed; 0 failed`) et `cargo test -- --nocapture` (suite complète verte).
- Remarque 9.9 : le chemin heureux d’exécution Wasm est désormais branché de bout en bout (`CLI/RPC -> VM -> storage contrat`), mais la gestion robuste des pannes d’exécution, des dépassements de gaz avec rollback atomique, des traps Wasm et des garanties transactionnelles de restauration d’état relève encore de l’étape 9.10.
- Remarque temps de test 9.9 : `cargo test --lib -- --nocapture` et `cargo test -- --nocapture` restent verts mais demeurent longs sur macOS à cause des tests confidentialité Groth16, avec plusieurs messages temporaires `has been running for over 60 seconds` ; ce comportement reste attendu dans le prototype actuel et n’indique pas une régression bloquante.
- Sauvegardes locales créées avant l’étape 9.8 :
  - `~/Documents/prims_sources/step-9.8/blockchain.types.rs.before_step_9_8`
  - `~/Documents/prims_sources/step-9.8/blockchain.validation.rs.before_step_9_8`
  - `~/Documents/prims_sources/step-9.8/api.mod.rs.before_step_9_8`
  - `~/Documents/prims_sources/step-9.8/prims-cli.rs.before_step_9_8`
  - `~/Documents/prims_sources/step-9.8/cli_integration.rs.before_step_9_8`
  - `~/Documents/prims_sources/step-9.8/journal_avancement.md.before_step_9_8`
  - `~/Documents/prims_sources/step-9.8/rpc_api.rs.before_step_9_8`
- Étape 9.8 validée : `src/blockchain/types.rs` contient désormais `TransactionType::CallContract` et la charge utile sérialisable `ContractCallPayload { method, params, gas_limit }` ; `src/api/mod.rs` accepte maintenant les transactions `CallContract` avec validations minimales cohérentes (`fee` fixe, `amount = 0`, `to` non vide, payload `bincode` valide, `method` non vide et `gas_limit > 0`) ; `src/bin/prims-cli.rs` branche réellement `call-contract` sur le RPC existant, sérialise les paramètres JSON en octets, construit une transaction signée `CallContract`, transmet un `gas_limit` configurable via `--gas-limit` (défaut `100000`) et affiche l’adresse de contrat, la méthode, les paramètres, le `tx_hash` et l’état d’acceptation ; les tests `tests/rpc_api.rs` couvrent désormais le chemin de succès et le rejet métier `gas_limit = 0`, et `tests/cli_integration.rs` valide l’intégration réelle de `call-contract` via le binaire `prims-cli` ; validations réussies avec `cargo fmt --all`, `cargo check`, `cargo test --test rpc_api -- --nocapture` (`5 passed; 0 failed`), `cargo test --test cli_integration -- --nocapture` (`7 passed; 0 failed`), `cargo test call_contract_ -- --nocapture` (`4 passed; 0 failed`) et `cargo test --lib -- --nocapture` (`169 passed; 0 failed`).
- Remarque 9.8 : `create-contract` et `call-contract` sont maintenant toutes deux supportées côté CLI/RPC au niveau des transactions natives, mais l’exécution effective du bytecode Wasm, l’instanciation du contrat, le branchement des host functions à l’exécution et la mise à jour réelle du storage relèvent encore de l’étape 9.9 ; la gestion des pannes, dépassements de gaz et rollbacks relèvera de l’étape 9.10.
- Remarque temps de test 9.8 : `cargo test --lib -- --nocapture` est resté vert mais a duré `631.31 s` sur macOS, avec plusieurs tests confidentialité Groth16 affichant temporairement `has been running for over 60 seconds` ; ce comportement reste attendu dans le prototype actuel et n’indique pas une régression bloquante.
- Sauvegardes locales créées avant l’étape 9.7 :
  - `~/Documents/prims_sources/step-9.7/blockchain.types.rs.before_step_9_7`
  - `~/Documents/prims_sources/step-9.7/storage.mod.rs.before_step_9_7`
  - `~/Documents/prims_sources/step-9.7/blockchain.validation.rs.before_step_9_7`
  - `~/Documents/prims_sources/step-9.7/prims-cli.rs.before_step_9_7`
  - `~/Documents/prims_sources/step-9.7/journal_avancement.md.before_step_9_7`
- Étape 9.7 validée : la transaction native `DeployContract` a été introduite dans `src/blockchain/types.rs` avec le support de l’adresse déterministe dérivée du hash du code et de l’envoyeur via `derive_contract_address(...)` et `contract_code_hash(...)` dans `src/blockchain/hash.rs` ; `src/api/mod.rs` accepte désormais `DeployContract` avec des validations minimales cohérentes (`fee` fixe, `amount = 0`, `to` non vide, bytecode Wasm présent dans `data`) ; `src/storage/mod.rs` expose maintenant `RocksDbStorage::deploy_contract(...)`, qui persiste le bytecode Wasm, initialise `storage_root` à `sha256([])` et crée le compte contrat avec `code_hash` ; `src/bin/prims-cli.rs` branche désormais réellement `create-contract <wasm_file>` sur le RPC existant, calcule l’adresse déterministe côté CLI, construit une transaction signée `DeployContract`, l’envoie via `send_transaction` et affiche l’adresse de contrat, le `tx_hash`, la taille Wasm et l’état d’acceptation ; validations réussies avec `cargo fmt --all`, `cargo check`, `cargo test deploy_contract_ -- --nocapture` (`3 passed; 0 failed`), `cargo test create_contract_command_submits_deploy_contract_transaction -- --nocapture` (`1 passed; 0 failed`), `cargo test --test cli_integration -- --nocapture` (`6 passed; 0 failed`) et `cargo test --lib -- --nocapture` (`168 passed; 0 failed`).
- Remarque 9.7 : `create-contract` a ouvert le support du déploiement côté CLI/RPC/stockage ; depuis l’étape 9.8, `call-contract` est désormais elle aussi supportée comme transaction native, tandis que l’exécution complète du module Wasm et les rollbacks restent à terminer dans les étapes 9.9 et 9.10.
- Remarque temps de test 9.7 : `cargo test --lib -- --nocapture` est resté vert mais a duré `670.06 s` sur macOS, avec plusieurs tests confidentialité Groth16 affichant temporairement `has been running for over 60 seconds` ; ce comportement est attendu dans le prototype actuel et n’indique pas une régression bloquante.
- Sauvegardes locales créées pendant l’étape 9.6 :
  - `~/Documents/prims_sources/step-9.6/vm.mod.rs.before_step_9_6`
  - `~/Documents/prims_sources/step-9.6/Cargo.toml.before_step_9_6`
  - `~/Documents/prims_sources/step-9.6/journal_avancement.md.before_step_9_6`
- Étape 9.6 validée : `src/vm/mod.rs` intègre désormais une première couche de comptabilisation du gaz Wasm avec le fuel natif de `wasmtime` ; la VM expose une configuration `WasmGasConfig`, une limite par défaut `DEFAULT_WASM_FUEL_LIMIT = 100_000`, un setter `set_fuel_limit` refusant `0`, un moteur configuré avec `Config::consume_fuel(true)`, un helper `new_metered_store` qui crédite le `Store` en fuel avant exécution, et un helper `remaining_fuel` pour observer le fuel restant après appel d’un module ; deux tests fonctionnels prouvent qu’un module Wasm fini consomme effectivement du fuel et qu’une boucle infinie est stoppée proprement par un trap `OutOfFuel` ; validations réussies avec `cargo fmt --all`, `cargo check`, `cargo test wasm_execution_ -- --nocapture` (`2 passed; 0 failed`), `cargo test vm:: -- --nocapture` (`11 passed; 0 failed`) et `cargo test --lib -- --nocapture` (`163 passed; 0 failed`).
- Remarque 9.6 : cette étape prépare l’exécution bornée des contrats, mais ne définit pas encore les transactions natives de déploiement / appel ni le branchement complet du runtime d’exécution ; ces points relèvent des étapes 9.7 à 9.10.
- Sauvegardes locales créées pendant l’étape 9.5 :
  - `~/Documents/prims_sources/step-9.5/blockchain.types.rs.before_step_9_5`
  - `~/Documents/prims_sources/step-9.5/storage.keys.rs.before_step_9_5`
  - `~/Documents/prims_sources/step-9.5/storage.mod.rs.before_step_9_5`
  - `~/Documents/prims_sources/step-9.5/vm.mod.rs.before_step_9_5`
  - `~/Documents/prims_sources/step-9.5/lib.rs.before_step_9_5`
  - `~/Documents/prims_sources/step-9.5/journal_avancement.md.before_step_9_5`
  - `~/Documents/prims_sources/step-9.5/blockchain.types.rs.after_step_9_5`
  - `~/Documents/prims_sources/step-9.5/storage.keys.rs.after_step_9_5`
  - `~/Documents/prims_sources/step-9.5/storage.mod.rs.after_step_9_5`
  - `~/Documents/prims_sources/step-9.5/vm.mod.rs.after_step_9_5`
- Étape 9.5 validée : le projet stocke désormais des métadonnées de contrat persistantes dans RocksDB via une table logique `contracts` portée par la clé `c:{address}` ; `src/blockchain/types.rs` expose `Contract { code_wasm, storage_root }`, `src/storage/keys.rs` ajoute `contract_key` et `contract_storage_prefix`, `src/storage/mod.rs` sait maintenant créer/lire un contrat et recalculer un `storage_root` déterministe à partir des slots `m:{address}:{key}`, et `src/vm/mod.rs` branche `set_storage` / `get_storage` sur ces helpers sans casser les protections 9.4 sur les comptes non-contrats ; validations réussies avec `cargo fmt --all`, `cargo check`, `cargo test contract_storage_roundtrip_updates_storage_root -- --nocapture` (`1 passed; 0 failed`), `cargo test set_storage_and_get_storage_roundtrip_under_contract_namespace -- --nocapture` (`1 passed; 0 failed`), `cargo test vm:: -- --nocapture` (`7 passed; 0 failed`) et `cargo test storage:: -- --nocapture` (`12 passed; 0 failed`).
- Remarque 9.5 : le `storage_root` actuel est un hash SHA-256 déterministe calculé sur les slots triés du namespace du contrat ; cela prépare l’étape de stockage d’état inspirée d’Ethereum sans introduire encore une Merkle Patricia Trie complète ni l’exécution réelle du bytecode Wasm, qui seront approfondies dans les étapes 9.6 à 9.9.
- Sauvegardes locales créées pendant l’étape 9.4 :
  - `~/Documents/prims_sources/step-9.4/vm.mod.rs.before_step_9_4`
  - `~/Documents/prims_sources/step-9.4/journal_avancement.md.before_step_9_4`
  - `~/Documents/prims_sources/step-9.4/vm.mod.rs.after_step_9_4`
  - `~/Documents/prims_sources/step-9.4/journal_avancement.md.before_step_9_4_update`
- Étape 9.4 validée : `src/vm/mod.rs` applique désormais un contrôle explicite des droits côté helpers Rust locaux avec `require_contract_address` et `require_contract_account` ; `transfer`, `set_storage` et `get_storage` refusent maintenant les comptes non-contrats en exigeant un compte présent en base avec `code_hash`, tout en conservant le namespace `contract_storage_key` pour le storage du contrat ; un test ciblé `set_storage_rejects_non_contract_accounts` a été ajouté ; validations réussies avec `cargo fmt --all --check`, `cargo check`, `cargo test vm:: -- --nocapture` (`7 passed; 0 failed`) et `cargo test set_storage_rejects_non_contract_accounts -- --nocapture` (`1 passed; 0 failed`).
- Remarque 9.4 : cette étape renforce les host functions côté API Rust locale de `WasmVM`, mais ne branche pas encore le stockage persistant du code Wasm, le chargement de contrats depuis RocksDB ni l’exécution complète d’un module `wasmtime` ; cela commence à l’étape 9.5.
- Sauvegardes locales créées pendant l’étape 9.3 :
  - `~/Documents/prims_sources/step-9.3/vm.mod.rs.before_step_9_3`
  - `~/Documents/prims_sources/step-9.3/lib.rs.before_step_9_3`
  - `~/Documents/prims_sources/step-9.3/journal_avancement.md.before_step_9_3`
  - `~/Documents/prims_sources/step-9.3/vm.mod.rs.after_step_9_3`
- Étape 9.3 validée : `src/vm/mod.rs` expose désormais `WasmExecutionContext`, `ContractEvent` et les sept host functions prévues par la roadmap (`get_balance`, `transfer`, `get_caller`, `get_block_height`, `set_storage`, `get_storage`, `emit_event`) ; `get_balance` lit le solde public via RocksDB, `transfer` prépare une transaction publique en file interne `pending_transfers`, `set_storage` / `get_storage` utilisent le namespace `contract_storage_key`, et `emit_event` conserve les événements émis en mémoire pour la suite du runtime ; validations réussies avec `cargo fmt --all --check`, `cargo check` et `cargo test vm:: -- --nocapture` (`6 passed; 0 failed`).
- Remarque 9.3 : cette étape définit l’API Rust et les comportements locaux de base des host functions, mais ne branche pas encore l’exécution réelle d’un module Wasm ni un contrôle complet des droits au niveau runtime ; cela reste l’objet de l’étape 9.4.
- Sauvegardes locales créées pendant l’étape 9.2 :
  - `~/Documents/prims_sources/step-9.2/lib.rs.before_step_9_2`
  - `~/Documents/prims_sources/step-9.2/lib.rs.after_step_9_2`
  - `~/Documents/prims_sources/step-9.2/vm.mod.rs.after_step_9_2`
  - `~/Documents/prims_sources/step-9.2/cargo-check.after_step_9_2.log`
  - `~/Documents/prims_sources/step-9.2/journal_avancement.md.before_step_9_2_update`
- Étape 9.2 validée : le dossier `src/vm/` existait déjà mais `src/vm/mod.rs` était absent ; un module minimal a été créé avec `WasmVM` et `WasmVM::new()`, puis `src/lib.rs` a été mis à jour pour exporter `pub mod vm;` ; validations réussies avec inspection de `src/vm/mod.rs` / `src/lib.rs` et `cargo check` (`Finished `dev` profile [unoptimized + debuginfo] target(s) in 2.27s`).
- Remarque 9.2 : cette étape pose uniquement le squelette du runtime Wasm ; aucune host function ni logique d’exécution de contrat n’est encore branchée à ce stade.

- Sauvegardes locales créées pendant l’étape 9.1 :
  - `~/Documents/prims_sources/step-9.1/Cargo.toml.before_step_9_1`
  - `~/Documents/prims_sources/step-9.1/Cargo.lock.before_step_9_1`
  - `~/Documents/prims_sources/step-9.1/journal_avancement.md.before_step_9_1`
- Étape 9.1 validée : ajout de `wasmtime` et `wasmtime-wasi` pour préparer le runtime Wasm ; `cargo add wasmtime@42.0.1 wasmtime-wasi@42.0.1` a mis à jour `Cargo.toml` et `Cargo.lock`, puis `cargo check` a confirmé l’intégration sans erreur.
- Remarque 9.1 : le dépôt Git local n’est pas encore exploitable comme référence de diff pour cette reprise ; la validation de l’étape repose donc sur l’inspection des fichiers modifiés et sur `cargo check`.
- Sauvegardes locales créées pendant l’étape 8.9 :
  - `~/Documents/prims_sources/step-8.9/api.mod.rs.before_step_8_9`
  - `~/Documents/prims_sources/step-8.9/api_rpc.md.before_step_8_9`
  - `~/Documents/prims_sources/step-8.9/rpc_api.rs.before_step_8_9`
  - `~/Documents/prims_sources/step-8.9/journal_avancement.md.before_step_8_9`
  - `~/Documents/prims_sources/step-8.9/api.mod.rs.after_step_8_9`
  - `~/Documents/prims_sources/step-8.9/api_rpc.md.after_step_8_9`
  - `~/Documents/prims_sources/step-8.9/rpc_api.rs.after_step_8_9`
- Étape 8.9 validée : l’API RPC publique n’expose plus `viewing_hint` via `get_balance` ; la documentation API a été alignée pour préciser que ce champ est volontairement omis ; le test `get_balance_omits_sensitive_viewing_hint_metadata` protège désormais cette propriété ; non-régression confirmée avec `rpc_api` (3 tests) et `cli_integration` (5 tests).
- Sauvegardes locales créées pendant l’étape 8.8 :
  - `~/Documents/prims_sources/step-8.8/Cargo.toml.before_step_8_8`
  - `~/Documents/prims_sources/step-8.8/journal_avancement.md.before_step_8_8`
  - `~/Documents/prims_sources/step-8.8/prims-cli.rs.before_step_8_8`
  - `~/Documents/prims_sources/step-8.8/rpc_api.rs.before_step_8_8`
  - `~/Documents/prims_sources/step-8.8/Cargo.toml.after_step_8_8`
  - `~/Documents/prims_sources/step-8.8/cli_integration.rs.after_step_8_8`
  - `~/Documents/prims_sources/step-8.8/prims-cli.rs.after_step_8_8`
  - `~/Documents/prims_sources/step-8.8/rpc_api.rs.after_step_8_8`
- Étape 8.8 validée : les tests d’intégration couvrent maintenant à la fois le serveur RPC et le CLI ; `tests/cli_integration.rs` lance un serveur JSON-RPC de test sur port éphémère et exécute le vrai binaire `prims-cli` contre lui avec des clés jetables passées via `PRIMS_SECRET_KEY_HEX`, sans utiliser de secret réel ni le fichier chiffré de test ; les commandes validées sont `balance`, `list-validators`, `send`, `stake` et `unstake`.
- Vérification finale 8.8 : `cargo test --test cli_integration -- --nocapture` passe avec `5 passed`, puis `cargo test --test rpc_api --test cli_integration -- --nocapture` passe avec `7 passed` au total (`5` CLI + `2` RPC).

- Sauvegardes locales créées pendant l étape 8.7 :
  - `~/Documents/prims_sources/step-8.7/journal_avancement.md.before_step_8_7`
  - `~/Documents/prims_sources/step-8.7/api.mod.rs.before_step_8_7`
  - `~/Documents/prims_sources/step-8.7/prims-cli.rs.before_step_8_7`
  - `~/Documents/prims_sources/step-8.7/prims-explorer.rs.before_step_8_7`
  - `~/Documents/prims_sources/step-8.7/README.md.before_step_8_7`
  - `~/Documents/prims_sources/step-8.7/api_rpc.md.after_step_8_7`
  - `~/Documents/prims_sources/step-8.7/README.md.after_step_8_7`
- Étape 8.7 validée : une documentation Markdown de l API JSON-RPC a été ajoutée dans `docs/api_rpc.md` avec les règles générales du protocole, les adresses par défaut (`127.0.0.1:7002` pour le RPC, `127.0.0.1:7003` pour l explorateur, `7001` côté P2P), la liste des méthodes exposées (`get_info`, `get_validators`, `get_note_commitments`, `get_balance`, `get_block`, `get_transaction`, `send_transaction`), des exemples de requêtes `curl`, les validations métier actuelles et la gestion des erreurs (`-32029` / `rate limit exceeded`) ; `README.md` contient désormais une section `Documentation` pointant vers `docs/api_rpc.md` et `docs/privacy_trusted_setup.md` ; validations finales réussies avec inspection structurée de `docs/api_rpc.md` et du `README.md`.
- Remarque doc 8.7 : la documentation retient le format Markdown plutôt qu OpenAPI pour cette étape, afin de rester strictement alignée sur l implémentation actuelle sans introduire de schéma spéculatif ; un export OpenAPI pourra être ajouté plus tard si le serveur RPC évolue.
- Sauvegardes locales créées pendant l’étape 8.6 :
  - `~/Documents/prims_sources/step-8.6/Cargo.toml.before_step_8_6`
  - `~/Documents/prims_sources/step-8.6/prims-cli.rs.before_step_8_6`
  - `~/Documents/prims_sources/step-8.6/journal_avancement.md.before_step_8_6`
  - `~/Documents/prims_sources/step-8.6/Cargo.toml.after_step_8_6`
  - `~/Documents/prims_sources/step-8.6/Cargo.lock.after_step_8_6`
  - `~/Documents/prims_sources/step-8.6/prims-cli.rs.after_step_8_6`
  - `~/Documents/prims_sources/step-8.6/test-wallet.age`
- Étape 8.6 validée : `src/bin/prims-cli.rs` gère maintenant le stockage sécurisé des clés avec chiffrement Age par mot de passe ; `generate-key --encrypted-file <path>` crée un fichier chiffré sans afficher la clé privée en clair, avec permissions restrictives `600` sur macOS ; `PRIMS_SECRET_KEY_FILE` charge désormais soit un fichier chiffré Age avec mot de passe interactif, soit les anciens formats plaintext JSON/hex pour compatibilité ascendante ; les buffers sensibles sont effacés avec `zeroize`, et un correctif a supprimé la double demande de mot de passe qui apparaissait initialement dans `stake` / `unstake` ; validations réussies avec `cargo check --bin prims-cli`, `cargo run --bin prims-cli -- generate-key --help`, création du fichier de test `~/Documents/prims_sources/step-8.6/test-wallet.age`, lecture réussie via `PRIMS_SECRET_KEY_FILE=... cargo run --bin prims-cli -- unstake` avec une seule invite de mot de passe puis erreur RPC métier cohérente `insufficient balance for fee`, et rejet correct d’un mauvais mot de passe avec `impossible de déchiffrer PRIMS_SECRET_KEY_FILE=...`.
- Remarque sécurité 8.6 : `~/Documents/prims_sources/step-8.6/test-wallet.age` contient une clé privée de test chiffrée ; ne jamais le partager, le committer ni le copier dans ChatGPT ; le conserver dans un emplacement privé ou le supprimer après usage de validation.
- Remarque compatibilité 8.6 : `prims-cli` reste compatible avec `PRIMS_SECRET_KEY_HEX` et les anciens fichiers plaintext JSON/hex via `PRIMS_SECRET_KEY_FILE`, mais le chemin recommandé pour la suite est désormais le fichier chiffré Age avec mot de passe interactif.
- Incident disque 8.6 corrigé : une relance de `cargo run --bin prims-cli -- unstake` a d’abord échoué au linkage avec `ld: write() failed, errno=28` ; le problème a été résolu par `cargo clean` (≈ `7.6 GiB` libérés) puis par la purge ciblée de `~/Library/Caches/Homebrew/downloads/*`, ce qui a remonté l’espace libre à environ `18 GiB` et permis la validation finale.
- Sauvegardes locales créées pendant l’étape 8.5 :
  - `~/Documents/prims_sources/step-8.5/Cargo.toml.before_step_8_5`
  - `~/Documents/prims_sources/step-8.5/prims-cli.rs.before_step_8_5`
  - `~/Documents/prims_sources/step-8.5/api.mod.rs.before_step_8_5`
  - `~/Documents/prims_sources/step-8.5/journal_avancement.md.before_step_8_5`
  - `~/Documents/prims_sources/step-8.5/prims-cli.rs.after_step_8_5`
  - `~/Documents/prims_sources/step-8.5/journal_avancement.md.before_step_8_5_update`
- Remarque 8.5 : `prims-cli` repose désormais sur le serveur RPC local et non sur le réseau P2P direct ; pour les commandes nécessitant une signature (`send`, `stake`, `unstake`), définir `PRIMS_SECRET_KEY_HEX` ou `PRIMS_SECRET_KEY_FILE` sans jamais afficher ni partager la clé privée.
- Remarque contrat 8.5 : les commandes `create-contract` et `call-contract` sont déjà exposées côté CLI pour respecter l’interface de la roadmap, mais restent volontairement non supportées tant qu’aucune transaction native ni méthode RPC dédiée aux contrats n’a encore été implémentée côté nœud.
- Sauvegardes locales créées pendant l’étape 8.4 :
  - `~/Documents/prims_sources/step-8.4/Cargo.toml.before_step_8_4`
  - `~/Documents/prims_sources/step-8.4/Cargo.lock.before_step_8_4`
  - `~/Documents/prims_sources/step-8.4/main.rs.before_step_8_4`
  - `~/Documents/prims_sources/step-8.4/lib.rs.before_step_8_4`
  - `~/Documents/prims_sources/step-8.4/api.mod.rs.before_step_8_4`
  - `~/Documents/prims_sources/step-8.4/prims-explorer.rs.before_step_8_4`
  - `~/Documents/prims_sources/step-8.4/Cargo.toml.after_step_8_4`
  - `~/Documents/prims_sources/step-8.4/Cargo.lock.after_step_8_4`
  - `~/Documents/prims_sources/step-8.4/prims-explorer.rs.after_step_8_4`
  - `~/Documents/prims_sources/step-8.4/node.log.after_step_8_4`
  - `~/Documents/prims_sources/step-8.4/explorer.log.after_step_8_4`
- Étape 8.4 validée : les dépendances web `axum`, `askama` et `reqwest` ont été ajoutées dans `Cargo.toml`, puis le binaire `src/bin/prims-explorer.rs` a remplacé le placeholder initial par un explorateur web minimal connecté au RPC existant ; il expose une page d’accueil avec recherche de solde, informations du nœud, validateurs et commitments anonymes, ainsi qu’une page `/address` affichant l’adresse demandée et la réponse RPC `get_balance` ; validations réussies avec `cargo check --bin prims-explorer`, démarrage réel du nœud principal via `cargo run --bin prims` (P2P `7001`, RPC `7002`), démarrage réel de l’explorateur via `cargo run --bin prims-explorer` sur `127.0.0.1:7003`, test HTTP `curl -I http://127.0.0.1:7003` retournant `HTTP/1.1 200 OK`, et vérifications HTML des routes `/` et `/address?address=demo-address`.
- Incident disque 8.4 corrigé : la première tentative de `cargo check --bin prims-explorer` a échoué pendant l’archivage de `librocksdb-sys` avec `ranlib: can't write to output file (No space left on device)` ; le diagnostic a montré seulement `116 MiB` libres sur `/`, un dossier `target/` de `7.4G` et un `Docker.raw` de `228G` ; le problème a été résolu proprement en supprimant `target/` (`rm -rf target`), ce qui a libéré environ `7.3 GiB` et permis la relance réussie de la compilation.
- Remarque usage 8.4 : l’explorateur web minimal utilise par défaut `PRIMS_RPC_URL=http://127.0.0.1:7002` et écoute sur `PRIMS_EXPLORER_ADDRESS=127.0.0.1:7003` ; il s’agit d’un binaire séparé (`cargo run --bin prims-explorer`) qui ne remplace pas le nœud principal `cargo run --bin prims`.
- Sauvegardes locales créées pendant l’étape 8.3 :
  - `~/Documents/prims_sources/step-8.3/Cargo.toml.before_step_8_3`
  - `~/Documents/prims_sources/step-8.3/Cargo.lock.before_step_8_3`
  - `~/Documents/prims_sources/step-8.3/api.mod.rs.before_step_8_3`
  - `~/Documents/prims_sources/step-8.3/main.rs.before_step_8_3`
  - `~/Documents/prims_sources/step-8.3/Cargo.toml.after_step_8_3`
  - `~/Documents/prims_sources/step-8.3/Cargo.lock.after_step_8_3`
  - `~/Documents/prims_sources/step-8.3/api.mod.rs.after_step_8_3`
  - `~/Documents/prims_sources/step-8.3/rpc_api.rs.after_step_8_3`
- Étape 8.3 validée : la dépendance `governor = "0.10"` a été ajoutée dans `Cargo.toml` puis intégrée dans `src/api/mod.rs` pour appliquer un rate limiting global au serveur RPC ; `RpcState` contient désormais un `DefaultDirectRateLimiter`, expose `with_rate_limit(...)` pour injecter un quota non nul personnalisé et applique `check_rate_limit()` au début de chaque méthode RPC (`get_block`, `get_transaction`, `send_transaction`, `get_balance`, `get_info`, `get_validators`, `get_note_commitments`) ; en cas de dépassement, le serveur renvoie l’erreur JSON-RPC dédiée `-32029` avec le message `rate limit exceeded` ; le quota par défaut actuel est fixé à `20` requêtes par seconde ; validations réussies avec `cargo check --lib`, `cargo check` et `cargo test --test rpc_api -- --nocapture`, ce dernier passant désormais avec `2 passed; 0 failed` grâce à l’ajout du test `get_validators_is_rate_limited_after_quota_is_exhausted`.
- Sauvegardes locales créées pendant l’étape 8.2 :
  - `~/Documents/prims_sources/step-8.2/Cargo.toml.before_step_8_2`
  - `~/Documents/prims_sources/step-8.2/lib.rs.before_step_8_2`
  - `~/Documents/prims_sources/step-8.2/main.rs.before_step_8_2`
  - `~/Documents/prims_sources/step-8.2/storage.mod.rs.before_step_8_2`
  - `~/Documents/prims_sources/step-8.2/blockchain.types.rs.before_step_8_2`
  - `~/Documents/prims_sources/step-8.2/privacy.mod.rs.before_step_8_2`
  - `~/Documents/prims_sources/step-8.2/consensus.mod.rs.before_async_fix`
  - `~/Documents/prims_sources/step-8.2/api.mod.rs.before_async_fix`
  - `~/Documents/prims_sources/step-8.2/main.rs.before_async_fix`
  - `~/Documents/prims_sources/step-8.2/Cargo.toml.after_step_8_2`
  - `~/Documents/prims_sources/step-8.2/Cargo.lock.after_step_8_2`
  - `~/Documents/prims_sources/step-8.2/lib.rs.after_step_8_2`
  - `~/Documents/prims_sources/step-8.2/main.rs.after_step_8_2`
  - `~/Documents/prims_sources/step-8.2/consensus.mod.rs.after_step_8_2`
  - `~/Documents/prims_sources/step-8.2/api.mod.rs.after_step_8_2`
  - `~/Documents/prims_sources/step-8.2/rpc_api.rs.after_step_8_2`
  - `~/Documents/prims_sources/step-8.2/cargo_check_step_8_2.pass1.final.log`
  - `~/Documents/prims_sources/step-8.2/cargo_check_step_8_2.pass2.final.log`
  - `~/Documents/prims_sources/step-8.2/cargo_check_step_8_2.bin.final.log`
  - `~/Documents/prims_sources/step-8.2/cargo_check_step_8_2.after_async_fix.final.log`
- Étape 8.2 validée : `src/api/mod.rs` a été créé pour exposer les méthodes RPC de base de Prims via `jsonrpsee`, avec conversion hexadécimale des entrées/sorties, lecture depuis RocksDB pour les blocs, transactions, comptes, validateurs et engagements de notes, et envoi des transactions valides vers la mempool ; `src/main.rs` démarre désormais un serveur RPC sur un port distinct du réseau P2P, affiche explicitement les méthodes disponibles et ouvre le stockage local RocksDB via `PRIMS_DB_PATH` ; `src/lib.rs` exporte désormais `pub mod api;` ; `src/consensus/mod.rs` contient maintenant `Mempool::len_async()` pour éviter le panic Tokio rencontré lors du premier appel réel à `get_info` ; le test d’intégration `tests/rpc_api.rs` valide le chemin de succès de `send_transaction` avec une transaction `bincode` sérialisée en hex, un compte créditeur en base et une insertion confirmée dans la mempool.
- Incident important 8.2 corrigé : le premier test réel de `get_info` via `curl` a déclenché un panic `Cannot start a runtime from within a runtime` car le RPC appelait `Mempool::len()` qui utilisait `Runtime::block_on` à l’intérieur du runtime Tokio déjà actif ; le problème a été corrigé en introduisant `Mempool::len_async()` puis en rendant asynchrones les chemins RPC concernés (`get_info` et le calcul de `mempool_size` dans `send_transaction`).
- Remarque usage 8.2 : le projet contient plusieurs binaires (`prims`, `prims-cli`, `prims-explorer`, `prims-node`, `prims-setup`) ; pour lancer le nœud principal avec le serveur RPC, utiliser `cargo run --bin prims`, pas `cargo run` seul.
- Remarque JSON-RPC 8.2 : les méthodes actuellement exposées attendent des paramètres nommés sous forme d’objet JSON (`params: {...}`) et non un tableau contenant un objet (`params: [{...}]`) ; par exemple `get_balance` doit être appelé avec `params":{"address":"..."}`.
- Sauvegardes locales créées pendant l’étape 8.1 :
  - `~/Documents/prims_sources/step-8.1/Cargo.toml.before_step_8_1`
  - `~/Documents/prims_sources/step-8.1/journal_avancement.md.before_step_8_1`
  - `~/Documents/prims_sources/step-8.1/cargo_check_step_8_1.log`
  - `~/Documents/prims_sources/step-8.1/Cargo.toml.after_step_8_1`
  - `~/Documents/prims_sources/step-8.1/Cargo.lock.after_step_8_1`
  - `~/Documents/prims_sources/step-8.1/cargo_check_step_8_1.after_step_8_1.log`
- Étape 8.1 validée : la dépendance `jsonrpsee` a été ajoutée pour préparer le serveur RPC de Prims, avec configuration minimale côté serveur dans `Cargo.toml` (`version = "0.26.0"`, `default-features = false`, `features = ["server"]`) ; `Cargo.lock` a été mis à jour avec `jsonrpsee`, `jsonrpsee-core`, `jsonrpsee-server`, `jsonrpsee-types`, `tokio-stream`, `tower` et dépendances associées ; la première tentative de `cargo check --lib` a échoué non pas à cause de `jsonrpsee`, mais par manque d’espace disque pendant l’archivage de `librocksdb-sys` (`ranlib: can't write to output file (No space left on device)`) ; le problème a été résolu proprement par `cargo clean` (`12743` fichiers supprimés, `8.3 GiB` libérés), puis la validation finale a réussi avec `cargo check --lib` et `cargo tree -p jsonrpsee`, confirmant l’intégration correcte de la pile RPC serveur sans régression de compilation.
- Incident disque 8.1 : la compilation de vérification a temporairement échoué sur macOS lors de la construction de `librocksdb-sys` à cause d’un manque d’espace disque ; ce point est désormais résolu, mais reste à surveiller lors des prochaines grosses compilations ou benchmarks si le dossier `target/` regrossit fortement.
- Sauvegardes locales créées pendant l’étape 7.13 :
  - `~/Documents/prims_sources/step-7.13/journal_avancement.md.before_step_7_13`
  - `~/Documents/prims_sources/step-7.13/privacy.mod.rs.before_step_7_13`
  - `~/Documents/prims_sources/step-7.13/privacy.conversion.rs.before_step_7_13`
  - `~/Documents/prims_sources/step-7.13/Cargo.toml.before_step_7_13`
  - `~/Documents/prims_sources/step-7.13/privacy.mod.rs.after_step_7_13`
  - `~/Documents/prims_sources/step-7.13/privacy.conversion.rs.after_step_7_13`
- Étape 7.13 validée : la sécurité de la phase confidentialité a été renforcée avec deux nouveaux tests ciblés ; `src/privacy/mod.rs` contient désormais `anon_transaction_rejects_truncated_serialized_proof`, qui vérifie qu’une preuve Groth16 sérialisée puis tronquée est rejetée avec une erreur de désérialisation, et `src/privacy/conversion.rs` contient désormais `anon_to_public_rejects_spending_same_note_twice_after_state_update`, qui valide qu’une note déjà consommée ne peut pas être reconvertie une seconde fois après mise à jour de `anonymous_state.note_commitments` ; validations réussies avec `cargo test anon_transaction_rejects_truncated_serialized_proof --lib` (`1 passed; 0 failed`), `cargo test anon_to_public_rejects_spending_same_note_twice_after_state_update --lib` (`1 passed; 0 failed`), `cargo test privacy:: --lib` (`36 passed; 0 failed`) et `CARGO_INCREMENTAL=0 RUSTFLAGS="-C debuginfo=0" cargo test --lib` (`150 passed; 0 failed`).
- Remarque 7.13 : dans le prototype actuel, la protection contre la double-dépense côté conversion `AnonToPublic` repose sur le retrait de l’engagement consommé de `account.anonymous_state.note_commitments`, combiné à la vérification de possession et de preuve de Merkle ; il n’existe pas encore de registre explicite de `nullifier`, ce qui reste cohérent avec la portée actuelle de la roadmap.
- Remarque temps de test 7.13 : plusieurs tests Groth16 affichent `has been running for over 60 seconds` en mode debug ; ce comportement est attendu dans ce prototype car ils régénèrent localement un setup et des preuves, et ne constitue pas une erreur bloquante.
- Sauvegardes locales créées pendant l’étape 7.12 :
  - `~/Documents/prims_sources/step-7.12/journal_avancement.md.before_step_7_12`
  - `~/Documents/prims_sources/step-7.12/privacy.mod.rs.before_step_7_12`
  - `~/Documents/prims_sources/step-7.12/privacy.mod.rs.after_failed_test_attempt`
  - `~/Documents/prims_sources/step-7.12/storage_benchmark.rs.before_step_7_12_fix`
  - `~/Documents/prims_sources/step-7.12/privacy_benchmark.log`
- Étape 7.12 validée : la mesure de performance a d’abord échoué en test unitaire debug (`~27 s` pour la génération de preuve), puis a été corrigée proprement en déplaçant la mesure dans un benchmark Criterion en build optimisé (`benches/privacy_benchmark.rs`) tout en conservant dans `src/privacy/mod.rs` un test fonctionnel `zk_transfer_proof_generation_and_verification_succeeds` ; résultats finaux observés avec `LC_ALL=C cargo bench --bench privacy_benchmark` : génération de preuve `privacy_zk_transfer_proof_generation` ≈ `1.5581–1.5642 s` et vérification `privacy_zk_transfer_proof_verification` ≈ `1.1821–1.1865 ms`, ce qui valide les objectifs de la roadmap `< 10 s` et `< 100 ms` ; validations complémentaires réussies avec `cargo bench --bench privacy_benchmark --no-run`, `cargo test zk_transfer_proof_generation_and_verification_succeeds --lib` (`1 passed; 0 failed`), `cargo test privacy:: --lib` (`34 passed; 0 failed`) et `CARGO_INCREMENTAL=0 RUSTFLAGS="-C debuginfo=0" cargo test --lib` (`148 passed; 0 failed`).
- Incident disque géré pendant l’étape 7.12 : `cargo bench --bench privacy_benchmark --no-run` a d’abord échoué avec `No space left on device (os error 28)` ; le problème a été résolu proprement par `cargo clean`, qui a supprimé `13567` fichiers et libéré `8.4 GiB`, permettant ensuite la compilation et l’exécution du benchmark release sans nouvelle erreur bloquante.
- Sauvegardes locales créées pendant l’étape 7.11 :
  - `~/Documents/prims_sources/step-7.11/journal_avancement.md.before_step_7_11`
  - `~/Documents/prims_sources/step-7.11/journal_avancement.md.before_step_7_11_update`
  - `~/Documents/prims_sources/step-7.11/privacy.conversion.rs.before_step_7_11`
  - `~/Documents/prims_sources/step-7.11/privacy.mod.rs.before_step_7_11`
  - `~/Documents/prims_sources/step-7.11/privacy.mod.rs.after_step_7_11`
- Étape 7.11 validée : un test d’intégration de non-traçabilité a été ajouté dans `src/privacy/mod.rs` avec `anon_transaction_public_inputs_do_not_reveal_which_same_value_note_was_spent` ; il valide qu’on peut construire deux transactions anonymes à partir de deux notes d’entrée différentes mais de même valeur, obtenir deux preuves distinctes, vérifier correctement chaque preuve, conserver les mêmes entrées publiques visibles (`merkle_root`, `fee`, `output_commitments`) et ne pas révéler publiquement quelle note a été dépensée ; validations réussies avec `cargo test anon_transaction_public_inputs_do_not_reveal_which_same_value_note_was_spent --lib` (`1 passed; 0 failed`), `cargo test privacy:: --lib` (`33 passed; 0 failed`) et `CARGO_INCREMENTAL=0 RUSTFLAGS="-C debuginfo=0" cargo test --lib` (`147 passed; 0 failed`).
- Remarque 7.11 : dans le setup simplifié actuel, le test de non-traçabilité vérifie chaque preuve avec la clé de vérification issue de son propre setup local, tout en conservant des entrées publiques identiques ; cela valide bien l’objectif fonctionnel de l’étape 7.11 sans remettre en cause les étapes 7.6 à 7.10.
- Sauvegardes locales créées pendant l’étape 7.10 :
  - `~/Documents/prims_sources/step-7.10/blockchain.types.rs.before_step_7_10`
  - `~/Documents/prims_sources/step-7.10/blockchain.validation.rs.before_step_7_10`
  - `~/Documents/prims_sources/step-7.10/journal_avancement.md.before_step_7_10`
  - `~/Documents/prims_sources/step-7.10/lib.rs.before_step_7_10`
  - `~/Documents/prims_sources/step-7.10/privacy.mod.rs.before_step_7_10`
  - `~/Documents/prims_sources/step-7.10/storage.mod.rs.before_step_7_10`
  - `~/Documents/prims_sources/step-7.10/blockchain.types.rs.after_step_7_10`
  - `~/Documents/prims_sources/step-7.10/privacy.mod.rs.after_step_7_10`
  - `~/Documents/prims_sources/step-7.10/privacy.conversion.rs.after_step_7_10`
- Étape 7.10 validée : conversion entre modèle public et modèle anonyme implémentée avec ajout de `TransactionType::{PublicToAnon, AnonToPublic}` dans `src/blockchain/types.rs`, nouveau sous-module `src/privacy/conversion.rs` exporté via `src/privacy/mod.rs`, structures `PublicToAnonRequest`, `AnonToPublicRequest`, `PublicToAnonConversion`, `AnonToPublicConversion` et erreur `ConversionError`, plus helpers `convert_public_to_anon` et `convert_anon_to_public` ; la conversion `PublicToAnon` débite le solde public, crée une `ViewableNote`, ajoute l’engagement dans `anonymous_state.note_commitments` et recalcule la racine Merkle ; la conversion `AnonToPublic` vérifie la possession via `derive_note_recipient_public_key`, valide la preuve de Merkle, retire l’engagement consommé et crédite le solde public ; validations réussies : `cargo test privacy::conversion:: --lib` (`5 passed; 0 failed`), `cargo test privacy:: --lib` (`32 passed; 0 failed`) et `CARGO_INCREMENTAL=0 RUSTFLAGS="-C debuginfo=0" cargo test --lib` (`146 passed; 0 failed`).
- Remarque sauvegarde 7.10 : les copies finales ont été renommées avec des noms explicites (`blockchain.types.rs`, `privacy.mod.rs`, `privacy.conversion.rs`) pour éviter tout écrasement silencieux de fichiers portant le même nom.
- Sauvegardes locales créées pendant l’étape 7.9 :
  - `~/Documents/prims_sources/step-7.9/journal_avancement.md`
  - `~/Documents/prims_sources/step-7.9/lib.rs`
  - `~/Documents/prims_sources/step-7.9/mod.rs`
  - `~/Documents/prims_sources/step-7.9/types.rs`
  - `~/Documents/prims_sources/step-7.9/validation.rs`
  - `~/Documents/prims_sources/step-7.9/keys.rs`
- Étape 7.9 validée : gestion de l’état anonyme implémentée avec `AnonymousAccountState` ajouté dans `src/blockchain/types.rs` pour attacher à chaque compte un `viewing_hint` optionnel et la liste de ses engagements de notes, `ViewableNote`, `NoteViewingKey` et `NoteViewingHint` ajoutés dans `src/privacy/mod.rs` pour permettre au destinataire de détecter les notes qui lui sont destinées via un hint dérivé SHA-256 (`prims-viewing-key`), nouvelles clés RocksDB `n:`, `v:` et `r:` ajoutées dans `src/storage/keys.rs`, stockage métier étendu dans `src/storage/mod.rs` avec `save_viewable_note`, `get_viewable_note`, `find_notes_for_viewing_key`, `save_note_merkle_root` et `get_note_merkle_root`, compatibilité des helpers/tests `Account` mise à jour dans `src/blockchain/validation.rs` ; tests dédiés validés `viewable_note_matches_its_viewing_key`, `viewable_note_rejects_zero_viewing_key`, `storage_saves_and_scans_viewable_notes_by_viewing_key`, `storage_persists_note_merkle_root_globally_and_by_shard` ; `cargo test privacy:: --lib` passe avec `27 passed; 0 failed`, `cargo test storage:: --lib` passe avec `11 passed; 0 failed` et `CARGO_INCREMENTAL=0 RUSTFLAGS="-C debuginfo=0" cargo test --lib` passe avec `141 passed; 0 failed`.
- Remarque sauvegarde 7.9 : `src/privacy/mod.rs` et `src/storage/mod.rs` partagent le même nom de fichier ; dans `~/Documents/prims_sources/step-7.9/`, un seul `mod.rs` est visible. Pour les prochaines sauvegardes, utiliser des noms distincts afin d’éviter tout écrasement silencieux.
- Sauvegardes locales créées pendant l’étape 7.8 :
  - `~/Documents/prims_sources/step-7.8/privacy.before_step_7_8`
  - `~/Documents/prims_sources/step-7.8/privacy.after_step_7_8`
  - `~/Documents/prims_sources/step-7.8/cargo_test_lib.after_step_7_8.log`
  - `~/Documents/prims_sources/step-7.8/journal_avancement.md.before_step_7_8_update`
- Étape 7.8 validée : type de transaction `AnonTransaction` ajouté dans `src/privacy/mod.rs` avec `merkle_root`, `fee`, `output_commitments` et `proof`, plus l’erreur dédiée `AnonTransactionError` et les helpers `new`, `public_inputs`, `decode_proof`, `verify` et `verify_with_prepared_key` ; la preuve Groth16 est sérialisée en format compressé via `ark-serialize` pour être transportée avec les engagements publics ; deux tests dédiés ont été ajoutés (`anon_transaction_verifies_with_matching_public_inputs`, `anon_transaction_rejects_tampered_fee`) pour valider qu’une transaction anonyme reconstruite depuis ses entrées publiques vérifie correctement et qu’un `fee` falsifié est rejeté ; `cargo test privacy:: --lib` passe avec `25 passed; 0 failed` et `CARGO_INCREMENTAL=0 RUSTFLAGS="-C debuginfo=0" cargo test --lib` passe avec `137 passed; 0 failed`.
- Sauvegardes locales créées pendant l’étape 7.7 :
  - `~/Documents/prims_sources/step-7.7/privacy.mod.rs.after_step_7_7`
  - `~/Documents/prims_sources/step-7.7/lib.rs.after_step_7_7`
  - `~/Documents/prims_sources/step-7.7/Cargo.toml.after_step_7_7`
  - `~/Documents/prims_sources/step-7.7/Cargo.lock.after_step_7_7`
  - `journal_avancement.md.before_step_7_7_update`
- Étape 7.7 validée : génération de preuve Groth16 hors chaîne et vérification côté nœud ajoutées dans `src/privacy/mod.rs` avec les aliases `ZkTransferProof`, `ZkTransferProvingKey`, `ZkTransferVerifyingKey` et `ZkTransferPreparedVerifyingKey`, l’erreur dédiée `ZkTransferProofSystemError`, et les helpers `zk_transfer_public_inputs_to_field_elements`, `prepare_zk_transfer_verifying_key`, `generate_zk_transfer_proof`, `verify_zk_transfer_proof` et `verify_zk_transfer_proof_with_prepared_key` ; l’encodage des entrées publiques a été aligné sur l’allocation arkworks via un `ConstraintSystem` pour garantir la compatibilité exacte avec la clé de vérification ; deux tests Groth16 ont été ajoutés pour valider qu’une preuve issue du circuit de référence est acceptée avec les bonnes entrées publiques et rejetée si les données publiques sont altérées (`fee` falsifié) ; `cargo test privacy:: --lib` passe avec `23 passed; 0 failed` et `CARGO_INCREMENTAL=0 RUSTFLAGS="-C debuginfo=0" cargo test --lib` passe avec `135 passed; 0 failed`.
- Sauvegardes locales créées pendant l’étape 7.6 :
  - `~/Documents/prims_sources/step-7.6/mod.rs`
  - `~/Documents/prims_sources/step-7.6/Cargo.toml`
  - `~/Documents/prims_sources/step-7.6/Cargo.lock`
  - `~/Documents/prims_sources/step-7.6/journal_avancement.md`
  - `~/Documents/prims_sources/step-7.6/privacy.before_step_7_6`
  - `~/Documents/prims_sources/step-7.6/Cargo.toml.before_step_7_6`
  - `~/Documents/prims_sources/step-7.6/Cargo.lock.before_step_7_6`
- Étape 7.6 validée : un trusted setup simplifié et documenté a été ajouté pour `ZkTransferCircuit` avec la dépendance `ark-serialize`, les nouveaux types `ZkTransferSetupCircuitMetadata` et `ZkTransferSetupError`, le helper `build_reference_zk_transfer_setup_circuit` dans `src/privacy/mod.rs`, et le binaire `src/bin/prims-setup.rs` exposant les sous-commandes `init`, `contribute`, `finalize` et `inspect` ; une cérémonie locale simplifiée à 2 contributions (`manu225`, `participant-local-2`) a été exécutée avec succès et a produit `artifacts/zk-setup/ceremony_state.json`, `groth16_proving_key.bin`, `groth16_verifying_key.bin` et `setup_metadata.json`, puis la documentation `docs/privacy_trusted_setup.md` a été ajoutée ; les paramètres générés sont explicitement liés à la forme de circuit de référence actuelle (`1` entrée, `2` sorties, profondeur Merkle `1`, `2` feuilles, frais `5`) et ce setup reste un prototype local non adapté à une cérémonie MPC de production ; `cargo check --bin prims-setup` passe, `cargo run --bin prims-setup -- --help` passe, `cargo test privacy:: --lib` passe avec `21 passed; 0 failed`, `CARGO_INCREMENTAL=0 RUSTFLAGS="-C debuginfo=0" cargo test --lib` passe avec `133 passed; 0 failed`, et `cargo run --bin prims-setup -- inspect --out-dir artifacts/zk-setup` confirme les `2` contributions et le digest final `b7fb50c1777b6ab09e2fcb4d4050190b68b3b908c18175229d9cfbf3b3ed35a7`.
- Sauvegardes locales créées pendant l’étape 7.5 :
  - `~/Documents/prims_sources/step-7.5/privacy.before_step_7_5`
  - `~/Documents/prims_sources/step-7.5/privacy.after_step_7_5`
  - `~/Documents/prims_sources/step-7.5/Cargo.toml.before_step_7_5`
  - `~/Documents/prims_sources/step-7.5/Cargo.toml.after_step_7_5`
  - `~/Documents/prims_sources/step-7.5/Cargo.lock.before_step_7_5`
  - `~/Documents/prims_sources/step-7.5/Cargo.lock.after_step_7_5`
  - `~/Documents/prims_sources/step-7.5/cargo_test_lib.after_step_7_5.log`
  - `journal_avancement.md.before_step_7_5_update`
- Étape 7.5 validée : `src/privacy/mod.rs` contient maintenant les briques d’un premier circuit zk-SNARK pour transferts anonymes avec témoin privé (`ZkTransferWitness`, `ZkNoteInputWitness`, `ZkNoteOutputWitness`), entrées publiques (`ZkTransferPublicInputs`), dérivation déterministe de clé publique depuis une clé privée de dépense via SHA-256 avec préfixe `prims-note-owner`, vérificateur natif de référence `verify_zk_transfer_public_inputs`, et circuit `ZkTransferCircuit` implémentant `ConstraintSynthesizer<Fr>` ; les contraintes couvrent l’appartenance des entrées à l’arbre de Merkle, la connaissance de la clé privée correspondant à la note, l’égalité `somme des entrées = somme des sorties + frais`, et la validité des engagements ; `Cargo.toml` et `Cargo.lock` ont été étendus avec `ark-relations`, `ark-r1cs-std` et `ark-crypto-primitives` ; les tests ciblés du vérificateur natif passent (`3 passed; 0 failed`), les tests ciblés du circuit passent (`2 passed; 0 failed`), tout le module `privacy` passe (`19 passed; 0 failed`) et la régression bibliothèque complète passe avec `133 passed; 0 failed`.
- Incident disque géré pendant l’étape 7.5 : un `rustc-LLVM ERROR: IO failure on output stream: No space left on device` a été résolu proprement par `cargo clean` (≈ `9.2 GiB` libérés), puis les validations finales ont été lancées avec `CARGO_INCREMENTAL=0 RUSTFLAGS="-C debuginfo=0"` pour réduire l’empreinte disque de compilation sous macOS.
- Sauvegardes locales créées pendant l’étape 7.4 :
  - `~/Documents/prims_sources/step-7.4/privacy.before_step_7_4`
  - `~/Documents/prims_sources/step-7.4/privacy.after_step_7_4`
  - `~/Documents/prims_sources/step-7.4/cargo_test_lib.after_step_7_4.log`
  - `journal_avancement.md.before_step_7_4_update`
- Étape 7.4 validée : `src/privacy/mod.rs` contient maintenant un arbre de Merkle minimal pour les notes anonymes, avec types explicites `NoteMerkleHash`, `NoteMerkleProof`, `NoteMerkleTree` et `NoteMerkleTreeError`, racine déterministe pour arbre vide, construction depuis des notes ou directement depuis des engagements, ajout incrémental via `append`, génération de preuve d’appartenance via `generate_proof` et vérification via `verify_proof` ; le hachage sépare explicitement les feuilles, les nœuds internes et l’arbre vide via les préfixes `prims-note-merkle-leaf`, `prims-note-merkle-node` et `prims-note-merkle-empty`, et les niveaux impairs du Merkle tree dupliquent le dernier nœud pour conserver une structure déterministe ; les tests ciblés Merkle passent (`6 passed; 0 failed`), tout le module `privacy` passe (`16 passed; 0 failed`), les tests préfixés `note_` restent verts (`12 passed; 0 failed`), et la régression bibliothèque complète passe avec `128 passed; 0 failed`.
- Sauvegardes locales créées pendant l’étape 7.3 :
  - `~/Documents/prims_sources/step-7.3/privacy.before_step_7_3`
  - `~/Documents/prims_sources/step-7.3/privacy.after_step_7_3`
  - `journal_avancement.md.before_step_7_3_update`
- Étape 7.3 validée : `src/privacy/mod.rs` contient maintenant une première `Note` anonyme minimale qui ne stocke que son `commitment`, avec un type explicite pour la clé publique destinataire (`NoteRecipientPublicKey`), un type explicite pour l’engagement (`NoteCommitment`), un constructeur `Note::new` et un calcul d’engagement `Note::compute_commitment` reposant sur SHA-256 appliqué à `value + recipient_public_key + blinding` ; des garde-fous rejettent les notes à valeur nulle, à clé publique nulle ou à facteur d’aveuglement nul via `NoteError` ; les tests ciblés `privacy::` passent avec `10 passed; 0 failed`, les 6 tests dédiés aux notes passent avec `cargo test privacy::tests::note_ --lib`, et la régression bibliothèque complète passe avec `122 passed; 0 failed`.
- Sauvegardes locales créées pendant l’étape 7.2 :
  - `~/Documents/prims_sources/step-7.2/journal_avancement.md.before_step_7_2`
  - `~/Documents/prims_sources/step-7.2/privacy.before_step_7_2`
  - `journal_avancement.md.before_step_7_2_update`
- Étape 7.2 validée : un premier modèle UTXO minimal a été ajouté dans `src/privacy/mod.rs` avec les structures `UtxoId`, `UtxoInput`, `UtxoOutput` et `UtxoTransaction`, ainsi qu’une validation de base via `validate_basic` pour refuser les transactions sans entrée, sans sortie ou avec sortie de montant nul ; le module `privacy` est désormais exporté par `src/lib.rs` ; les 4 tests ciblés `privacy::tests` passent, et la régression bibliothèque complète passe aussi avec `116 passed; 0 failed`.
- Sauvegardes locales créées pendant l’étape 7.1 :
  - `~/Documents/prims_sources/step-7.1/Cargo.toml.before_step_7_1`
  - `journal_avancement.md.before_step_7_1_update`
- Étape 7.1 validée : les dépendances `ark-*` nécessaires au démarrage de la phase confidentialité ont été ajoutées dans `Cargo.toml` avec des versions cohérentes (`0.5.0`) ; `cargo check` passe sur macOS, et `Cargo.lock` contient bien `ark-ff`, `ark-ec`, `ark-groth16`, `ark-bls12-381` et `ark-std`.
- Sauvegardes locales créées pendant l’étape 6.9 :
  - `~/Documents/prims_sources/step-6.9/journal_avancement.md`
  - `~/Documents/prims_sources/step-6.9/mod.rs`
  - `~/Documents/prims_sources/step-6.9/mod.rs.after_step_6_9`
- Étape 6.9 validée : un test de sécurité `compromised_shard_state_root_does_not_affect_other_shards` a été ajouté dans `src/sharding/mod.rs` pour simuler la compromission d’un shard via une mise à jour de racine d’état compromise et vérifier que les autres shards conservent leurs racines intactes ; le test ciblé passe (`1 passed`), tout le module `sharding::tests` passe (`21 passed; 0 failed`) et la suite globale `cargo test` passe (`112 passed; 0 failed`).
- Sauvegardes locales créées pendant l’étape 6.8 :
  - `journal_avancement.md.before_step_6_8_update`
  - `~/Documents/prims_sources/step-6.8/Dockerfile`
  - `~/Documents/prims_sources/step-6.8/docker-compose.yml`
  - `~/Documents/prims_sources/step-6.8/main.rs`
  - `~/Documents/prims_sources/step-6.8/mod.rs`
  - `~/Documents/prims_sources/step-6.8/prims-cli.rs`
  - `~/Documents/prims_sources/step-6.8/test_transaction_load.sh`
  - `~/Documents/prims_sources/step-6.8/benchmark_shards_tps.sh.before_fix`
  - `~/Documents/prims_sources/step-6.8/benchmark_shards_tps.sh`
  - `~/Documents/prims_sources/step-6.8/results.csv`
  - `~/Documents/prims_sources/step-6.8/benchmark_run_latest/`
- Étape 6.8 validée : le script `scripts/benchmark_shards_tps.sh` a été ajouté puis corrigé pour forcer un format numérique stable sous macOS via `LC_ALL=C`, conserver `published_tps` comme métrique principale et `dispatched_tps` comme métrique de diagnostic, et benchmarker automatiquement les scénarios Docker `1`, `2` et `3` shards ; résultats finaux observés : `1` shard → `400` transactions publiées en `7.445 s`, soit `53.73 TPS`, `0` transaction dispatchée (`0.00 dispatched_tps`) ; `2` shards → `800` transactions publiées en `7.108 s`, soit `112.55 TPS`, `400` transactions dispatchées (`56.27 dispatched_tps`) ; `3` shards → `1200` transactions publiées en `7.086 s`, soit `169.35 TPS`, `800` transactions dispatchées (`112.90 dispatched_tps`) ; les fichiers de résultats et logs ont été enregistrés dans `logs/benchmark_shards_tps/20260319_235957/` puis copiés dans `~/Documents/prims_sources/step-6.8/`. Point technique à surveiller : dans ce setup Docker, `shard1` accepte bien les publications clientes sur `7001`, mais ne journalise toujours pas `Dispatched transaction`, donc `dispatched_count` reste partiellement diagnostique pour l’instant.
- Sauvegardes locales créées pendant l’étape 6.7 :
  - `~/Documents/prims_sources/step-6.7/Cargo.toml`
  - `~/Documents/prims_sources/step-6.7/lib.rs`
  - `~/Documents/prims_sources/step-6.7/main.rs`
  - `~/Documents/prims_sources/step-6.7/mod.rs`
  - `~/Documents/prims_sources/step-6.7/Dockerfile.before_fix_benches`
  - `~/Documents/prims_sources/step-6.7/Dockerfile.before_fix_libclang`
  - `~/Documents/prims_sources/step-6.7/Dockerfile.before_fix_glibc`
  - `~/Documents/prims_sources/step-6.7/Dockerfile`
  - `~/Documents/prims_sources/step-6.7/docker-compose.yml`
  - `~/Documents/prims_sources/step-6.7/docker-compose.logs.after_step_6_7.txt`
- Étape 6.7 validée : `Dockerfile` et `docker-compose.yml` ont été ajoutés à la racine du projet pour simuler plusieurs shards avec Docker ; `docker compose build` construit les 3 images `prims-shard1`, `prims-shard2` et `prims-shard3`, `docker compose up -d` lance `prims-shard1-1`, `prims-shard2-1` et `prims-shard3-1`, `docker compose ps` confirme les 3 services `Up` avec publication des ports `7001`, `7002` et `7003`, et `docker compose logs --no-color --tail=200` confirme `PRIMS node started`, `Listening on ...`, `Discovered(...)` et `NewExternalAddrOfPeer ...` entre les shards. Les seeds Docker en `/dns4/...` déclenchent encore `MultiaddrNotSupported(...)`, mais la découverte inter-pairs fonctionne quand même via les adresses `/ip4/172.18.0.x/...` du réseau Docker.
- Environnement Docker stabilisé pendant l’étape 6.7 : Docker Desktop a été installé, `UseContainerdSnapshotter` a été désactivé dans `~/Library/Group Containers/group.com.docker/settings-store.json`, et le fichier `~/Library/Containers/com.docker.docker/Data/vms/0/data/Docker.raw` saturé a été supprimé puis recréé automatiquement après relance de Docker Desktop, ce qui a résolu les erreurs `input/output error`, `Docker Desktop is unable to start` et le manque d’espace disque observés pendant les builds.
- Sauvegardes locales créées pendant l’étape 6.6 :
  - `journal_avancement.md.before_step_6_6_update`
  - `~/Documents/prims_sources/step-6.6/journal_avancement.md`
  - `~/Documents/prims_sources/step-6.6/lib.rs`
  - `~/Documents/prims_sources/step-6.6/mod.rs`
  - `~/Documents/prims_sources/step-6.6/mod.rs.after_step_6_6`
- Étape 6.6 validée : `src/sharding/mod.rs` expose maintenant `update_shard_state_root_from_block` pour refléter sur la beacon chain la racine d’état issue du `merkle_root` d’un bloc finalisé de shard, avec conservation correcte des racines par epoch ; les tests `beacon_chain_updates_shard_state_root_from_block` et `beacon_chain_tracks_shard_state_updates_across_epochs` sont validés ; `cargo test sharding::tests --lib` passe avec `20 passed`, `cargo test --lib` passe avec `111 passed; 0 failed`, et `cargo test` global est valide sans échec.
- Sauvegardes locales créées pendant l’étape 6.5 :
  - `journal_avancement.md.before_step_6_5_update`
  - `~/Documents/prims_sources/step-6.5/lib.rs`
  - `~/Documents/prims_sources/step-6.5/mod.rs`
  - `~/Documents/prims_sources/step-6.5/types.rs`
  - `~/Documents/prims_sources/step-6.5/sharding_mod.final.rs`
- Étape 6.5 validée : `src/sharding/mod.rs` vérifie maintenant explicitement les `CrossShardReceipt` via `validate_cross_shard_receipt`, contrôle la cohérence complète d’une exécution cross-shard via `validate_cross_shard_execution_proofs`, et expose la finalité globale via `is_cross_shard_execution_globally_finalized` ; les erreurs `InvalidReceiptCount`, `MissingReceiptPhase`, `ReceiptTxHashMismatch`, `ReceiptRouteMismatch` et `InvalidReceiptProof` couvrent les falsifications et incohérences de preuves ; les tests `beacon_chain_validates_cross_shard_proofs_and_global_finality`, `cross_shard_global_finality_is_false_until_commit_phase` et `beacon_chain_rejects_tampered_cross_shard_receipt_proof` sont validés ; `cargo test sharding::tests --lib` passe avec `18 passed`, `cargo test --lib` passe avec `109 passed; 0 failed`, `cargo test` global est valide sans échec, et `cargo fmt --check` est vert.
- Sauvegardes locales créées pendant l’étape 6.4 :
  - `src/sharding/mod.rs.step-6.4.bak`
  - `src/blockchain/types.rs.step-6.4.bak`
  - `src/blockchain/validation.rs.step-6.4.bak`
  - `~/Documents/prims_sources/step-6.4/lib.rs`
  - `~/Documents/prims_sources/step-6.4/sharding_mod.rs`
  - `~/Documents/prims_sources/step-6.4/blockchain_types.rs`
  - `~/Documents/prims_sources/step-6.4/blockchain_validation.rs`
  - `~/Documents/prims_sources/step-6.4/sharding_mod.final.rs`
  - `~/Documents/prims_sources/step-6.4/blockchain_types.final.rs`
  - `~/Documents/prims_sources/step-6.4/blockchain_validation.final.rs`
  - `~/Documents/prims_sources/step-6.4/consensus_mod.final.rs`
  - `~/Documents/prims_sources/step-6.4/network_node.final.rs`
  - `~/Documents/prims_sources/step-6.4/storage_mod.final.rs`
  - `~/Documents/prims_sources/step-6.4/crypto_mod.final.rs`
  - `~/Documents/prims_sources/step-6.4/prims-cli.final.rs`
- Étape 6.4 validée : `src/sharding/mod.rs` introduit `CrossShardExecution` et `CrossShardTransactionError`, met en place le flux `prepare -> validate -> commit` pour les transactions cross-shard, génère des `CrossShardReceipt` avec preuves déterministes et permet leur inclusion dans `Block.receipts` ; `src/blockchain/types.rs` étend `Transaction` avec `source_shard` / `destination_shard`, ajoute `CrossShardPhase`, `CrossShardReceipt` et `Transaction::is_cross_shard`, tandis que les modules `validation`, `consensus`, `network`, `storage`, `crypto` et `prims-cli` ont été ajustés pour rester compatibles ; `cargo test sharding::tests --lib` passe avec `15 passed`, `cargo test --lib` passe avec `106 passed; 0 failed`, et `cargo test` global est valide sans échec ni warning.
- Contrôle global post-6.4 validé : `rustc`, `cargo`, `git` et `cargo audit` répondent correctement, `cargo check --tests`, `cargo test --lib` et `cargo test` sont verts, l’espace disque disponible est remonté à environ `11 GiB`, et `cargo audit` remonte deux avertissements non bloquants à surveiller (`bincode 1.3.3` dépendance directe non maintenue, `paste 1.0.15` dépendance indirecte via `libp2p`).
- État Git local constaté après l’étape 6.4 : le dossier `/Users/manu225/Documents/prims` contient bien un dépôt `.git`, mais sans commit local et sans remote configuré ; à régulariser plus tard avant synchronisation GitHub.
- Sauvegardes locales créées pendant l’étape 6.3 :
  - `journal_avancement.md.before_step_6_3_update`
  - `src/sharding/mod.rs.step-6.3.bak`
  - `src/consensus/mod.rs.step-6.3.bak`
  - `~/Documents/prims_sources/step-6.3/lib.rs`
  - `~/Documents/prims_sources/step-6.3/mod.rs`
  - `~/Documents/prims_sources/step-6.3/sharding_mod.rs`
  - `~/Documents/prims_sources/step-6.3/consensus_mod.rs`
- Étape 6.3 validée : `src/sharding/mod.rs` introduit `ShardConsensus` et `ShardConsensusError`, rattache chaque shard à son comité effectif de validateurs à partir de `BeaconChain`, réutilise les votes signés, la collecte pondérée et la règle de finalisation de la phase 4, et rejette les votes hors comité ; `cargo test sharding::tests --lib` passe avec `11 passed`, `cargo test --lib` passe avec `101 passed; 0 failed`, et `cargo test` global est valide sans échec.
- Sauvegardes locales créées pendant l’étape 6.2 :
  - `journal_avancement.md.before_step_6_2_update`
  - `~/Documents/prims_sources/step-6.2/lib.rs`
  - `~/Documents/prims_sources/step-6.2/mod.rs`
- Étape 6.2 validée : `src/sharding/mod.rs` introduit `BeaconChain`, `ShardCommitteeAssignment` et `ShardStateRoot`, gère l’ensemble des validateurs actifs, assigne les comités de validateurs par epoch de manière déterministe à partir de l’epoch, initialise et met à jour les racines d’état des shards ; `cargo test sharding::tests --lib` passe avec `8 passed`, `cargo test --lib` passe avec `98 passed; 0 failed`, et `cargo test` global est valide sans échec.
- Sauvegardes locales créées pendant l’étape 6.1 :
  - `src/lib.rs.step-6.1.bak`
  - `journal_avancement.md.before_step_6_1_update`
  - `~/Documents/prims_sources/step-6.1/lib.rs`
  - `~/Documents/prims_sources/step-6.1/lib.rs.step-6.1.bak`
  - `~/Documents/prims_sources/step-6.1/mod.rs`
- Étape 6.1 validée : `src/sharding/mod.rs` introduit `ShardingConfig` avec `DEFAULT_INITIAL_SHARD_COUNT = 64`, validation d’un nombre de shards en puissance de 2, et mise à jour future via `apply_governance_update` ; `src/lib.rs` exporte désormais `pub mod sharding;` ; `cargo test --lib` passe avec `93 passed; 0 failed` et `cargo test` global est valide sans échec.
- Sauvegardes locales créées pendant l’étape 5.9 :
  - `src/consensus/mod.rs.step-5.9.bak`
  - `journal_avancement.md.before_step_5_9_update`
- Étape 5.9 validée : le test confirme qu’il n’existe pas d’ordre global de soumission visible conservé dans le bloc proposé, ce qui empêche le scénario sandwich testé `attaquant avant / victime / attaquant après`.
- Sauvegardes locales créées pendant l’étape 5.8 :
  - `benches/storage_benchmark.rs.step-5.8.bak`
  - `scripts/test_transaction_load.sh.step-5.8.bak`
  - `scripts/test_transaction_load.sh.step-5.8-release.bak`
  - `scripts/test_transaction_load.sh.step-5.8-timing.bak`
  - `src/network/gossip.rs.step-5.8.bak`
  - `src/network/behaviour.rs.step-5.8.bak`
  - `src/bin/prims-cli.rs.step-5.8-relay.bak`
  - `src/bin/prims-cli.rs.step-5.8-logging.bak`
  - `src/main.rs.step-5.8-logging.bak`
  - `src/bin/prims-cli.rs.step-5.8-queue.bak`
- Étape 5.8 validée : l’objectif de `10 000 TPS` en local est dépassé avec `10497,90 TPS` sur MacBook M4 16 Go RAM, mesuré avec `14` clients concurrents et `70000` transactions traitées sans échec client.
- Correctifs importants réalisés pendant l’étape 5.8 :
  - passage du script de charge en build `--release`
  - désabonnement des clients CLI aux topics Gossipsub non nécessaires pour éviter les effets `SlowPeer`
  - retrait des logs détaillés de `message_id`
  - simplification des logs `Dispatched transaction` et `Published transaction nonce`
  - ajout d’un retry spécifique sur `AllQueuesFull`
  - remplacement de la mesure en secondes entières par une mesure en millisecondes
  - stabilisation du comptage final des transactions dispatchées avant calcul du TPS
- Fichiers de résultats de benchmark étape 5.8 :
  - `logs/transaction_load_test/summary.last.txt`
  - `logs/transaction_load_test/summary.12x5000.txt`
  - `logs/transaction_load_test/summary.14x5000.txt`
- Sauvegardes locales créées pendant l’étape 5.7 :
  - `scripts/test_transaction_load.sh.step-5.7.bak`
  - `src/network/node.rs.step-5.7.bak`
  - `src/bin/prims-cli.rs.step-5.7.bak`
  - `src/consensus/mod.rs.step-5.7.bak`
  - `src/main.rs.step-5.7.bak`
  - `journal_avancement.md.before_step_5_7_update`
- Étape 5.7 validée : le test de charge avec milliers de transactions concurrentes fonctionne désormais en local ; le nœud reçoit bien les transactions réseau et les injecte dans la mempool sans panic Tokio.
- Correctifs importants réalisés pendant l’étape 5.7 :
  - création de `scripts/test_transaction_load.sh`
  - ajout de la commande `flood` dans `prims-cli`
  - ajout/usage du helper réseau de publication de transaction
  - correction du dispatch asynchrone vers la mempool côté nœud
  - suppression d’un accès synchrone problématique à la mempool dans le log de dispatch
- Sauvegardes locales créées pendant l’étape 5.6 :
  - `src/consensus/mod.rs.step-5.6.bak`
  - `journal_avancement.md.before_step_5_6_update`
- Étape 5.6 validée : la mempool n’inclut plus de 100 transactions d’une même adresse dans un même bloc ; l’excédent reste en attente pour les blocs suivants.
- Étape 5.5 validée : les frais de transaction sont désormais fixes via `FIXED_TRANSACTION_FEE`, sans priorité par frais, et les tests globaux passent à 88/88.
- Sauvegardes locales créées pendant l’étape 5.5 :
  - `src/blockchain/types.rs.step-5.5.bak`
  - `src/blockchain/validation.rs.step-5.5.bak`
  - `src/consensus/mod.rs.step-5.5.bak`
  - `journal_avancement.md.before_step_5_5_update`
- `roadmap.md` a été restauré localement pendant l’étape 5.5.
- Le vrai dossier de travail utilisé pour cette reprise est `/Users/manu225/Documents/prims`. Un autre dossier `~/prims` existe mais ne correspond pas à l’état courant du projet.
- Le token GitHub est configuré localement. Ne jamais le coller dans le terminal, dans le code source, dans un commit, ni dans ChatGPT.
- Les commandes doivent toujours être vérifiées avant de passer à l’étape suivante.
- `cargo run` sans option échoue car plusieurs binaires existent (`prims`, `prims-cli`, `prims-explorer`, `prims-node`) ; utiliser `cargo run --bin prims`.
- La structure initiale du projet est en place : `src/bin`, `src/network`, `src/blockchain`, `src/crypto`, `src/consensus`, `src/storage`, `src/vm`, `src/api`, `src/sharding`, `src/privacy`, `src/utils`, `tests`, `benches`, `docs`, `scripts`.
- `cargo audit` a signalé un avertissement : `bincode 1.3.3` est indiqué comme non maintenu (`RUSTSEC-2025-0141`). À surveiller lors du remplacement futur du format de sérialisation.
- Le port d’écoute par défaut a été déplacé de `7000` vers `7001` car `7000` était déjà utilisé sur la machine.
- Le `main` utilise maintenant `#[tokio::main]` pour exécuter correctement `libp2p` avec `.with_tokio()`.
- La configuration réseau lit désormais :
  - `PRIMS_LISTEN_ADDRESS` pour l’adresse d’écoute
  - `PRIMS_SEED_NODES` pour la liste de multiadresses des seed nodes
  - `PRIMS_PUBLISH_MESSAGE` pour publier un message de test Gossipsub
- La découverte locale a été validée avec deux nœuds :
  - nœud 1 sur `7001`
  - nœud 2 sur `7002`
  - connexion établie via seed node local
- Gossipsub a été validé :
  - topic utilisé : `prims-testnet`
  - publication test réussie : `hello-prims`
  - réception du message confirmée sur l’autre nœud
- Le fichier `src/network/message.rs` a été créé, relié via `pub mod message;` dans `src/network/mod.rs`, puis validé avec le test `network::message::tests::message_roundtrip_with_bincode`.
- `ConnectionLimits` est configurable via variables d’environnement (`PRIMS_MAX_ESTABLISHED_INCOMING`, `PRIMS_MAX_ESTABLISHED_OUTGOING`, `PRIMS_RECONNECT_DELAY_SECS`, `PRIMS_TEMPORARY_BAN_SECS`, `PRIMS_MAX_INVALID_MESSAGES`).
- `PeerPenalty` suit les déconnexions, les messages invalides et le bannissement temporaire.
- Les tests validés pour l’étape 1.6 couvrent les limites de connexion, la reconnexion et le bannissement temporaire dans `src/network/connection.rs` et `src/network/node.rs`.
- Le script `scripts/run_local_cluster.sh` lance 3 nœuds locaux, collecte leurs logs dans `logs/local_cluster/` et permet de vérifier visuellement la découverte des pairs et la propagation d’un message.
- L’instrumentation de benchmark affiche `Published at ms`, `Received at ms` et `Propagation latency ms` pour mesurer la latence de propagation.
- Benchmark 1.8 validé sur cluster local : `Published at ms: 1773597221174`, `Received at ms: 1773597221175`, `Propagation latency ms: 1`.
- Le script `scripts/test_connection_spam.sh` utilise maintenant `nc -w 1 -z` sur macOS et un envoi séquentiel pour éviter les blocages.
- Test DoS 1.9 validé : `Tentatives demandées : 60`, `Connexions TCP réussies : 60`, `Connexions TCP échouées : 0`.
- Pendant le test DoS, le nœud a journalisé des `IncomingConnection` suivis de `IncomingConnectionError` avec `Select(Failed)` ; ce comportement est attendu pour du spam TCP brut non conforme au handshake `libp2p`.
- Étape 2.1 validée : structures `Transaction`, `BlockHeader`, `Block` et `Account` créées dans `src/blockchain/types.rs`, exportées via `src/blockchain/mod.rs` et `src/lib.rs`, avec test de sérialisation/désérialisation `bincode` réussi.
- Étape 2.2 validée : dépendance `sha2` ajoutée, fonctions `sha256`, `hash_transaction`, `hash_block_header` et `calculate_merkle_root` créées dans `src/blockchain/hash.rs`, avec tests unitaires réussis.
- Étape 2.3 validée : `rocksdb` intégré, abstraction de stockage créée dans `src/storage/mod.rs`, ouverture de base testée avec succès via `opens_rocksdb_storage`.
- Étape 2.4 validée : préfixes de clés centralisés dans `src/storage/keys.rs` et vérifiés par le test `storage_key_prefixes_match_roadmap`.
- Étape 2.5 validée : opérations de base RocksDB (`put`, `get`, `delete`, `iter`) disponibles dans `src/storage/mod.rs`, avec test unitaire `storage_put_get_delete_and_iter` réussi.
- Étape 2.6 validée : persistance haut niveau des blocs, transactions et comptes ajoutée dans `src/storage/mod.rs`, avec vérification de l’index inverse `h:{hash}` dans le test `storage_high_level_block_transaction_account_roundtrip`.
- Étape 2.7 validée : les blocs, transactions et comptes persistent bien après fermeture/réouverture de RocksDB, test `storage_persists_after_restart` réussi.
- Étape 2.8 validée : benchmark RocksDB exécuté avec succès ; les temps mesurés pour 10 000 blocs restent très en dessous du seuil cible de la roadmap.
- Étape 2.9 validée : encapsulation des données critiques avec checksum SHA-256 dans `src/storage/checksum.rs`, détection de corruption confirmée par les tests.
- Étape 3.1 validée : génération de clés ed25519 opérationnelle dans `src/crypto/mod.rs`. En environnement disque serré, le test a été validé avec `CARGO_PROFILE_TEST_DEBUG=0` pour réduire la taille du linkage.
- Étape 3.2 validée : signature et vérification ed25519 des transactions opérationnelles dans `src/crypto/mod.rs`. Ne jamais afficher, coller ni stocker une vraie clé privée dans le terminal, le code ou ChatGPT.
- Étape 3.4 validée : les blocs sont maintenant vérifiés côté intégrité (`previous_hash`, `merkle_root`) et authenticité (signature du validateur).
- Étape 3.5 validée : la protection anti-replay est en place avec refus des nonces égaux ou inférieurs au dernier nonce enregistré.
- Étape 3.6 validée : les transactions refusent désormais les montants nuls, les débordements et les soldes insuffisants.
- Étape 3.7 validée : les transactions >= 1 Mo et les blocs >= 10 Mo sont désormais rejetés.
- Étape 3.8 validée : les tests confirment la signature, la vérification et le rejet des données altérées/corrompues.
- Étape 3.9 validée : les tests confirment le comportement attendu des hashes et l’absence d’anomalies évidentes sur les entrées distinctes et l’ordre des transactions.
- Étape 4.1 validée : les validateurs disposent désormais d’une structure dédiée et d’une persistance en base.
- Étape 4.2 validée : les transactions `Stake` et `Unstake` sont désormais modélisées proprement via `TransactionType`.
- Étape 4.3 validée : la sélection du proposant est maintenant déterministe, pondérée par le stake et stable quel que soit l’ordre des validateurs.
- Étape 4.4 validée : la mempool, la construction locale d’un bloc signé et la préparation de sa diffusion réseau sont en place.
- Étape 4.5 validée : les votes signés des validateurs sont maintenant vérifiables, collectés et agrégés selon le stake.
- Étape 4.6 validée : un bloc n’est finalisé que si les votes favorables dépassent strictement les deux tiers du stake actif.
- Étape 4.7 validée : en cas de fork, la sélection retient désormais la chaîne la plus lourde en stake cumulé.
- Étape 4.8 validée : les double-votes sont maintenant détectés par preuve et sanctionnés par une réduction de stake.
- Étape 4.9 validée : les récompenses combinent désormais les frais des transactions et une inflation annuelle de 2%, réparties entre les validateurs votants.
- Étape 4.10 validée : un scénario intégré à 4 validateurs confirme désormais la sélection, la gestion de fork et le slashing.
- Étape 4.11 validée : le test de benchmark confirme une finalisation en moins de 2 secondes sur le scénario courant.
- Étape 4.12 validée : le scénario byzantin à 33% confirme qu’une minorité byzantine ne peut ni finaliser un fork concurrent ni supplanter la chaîne honnête.
- Étape 5.1 validée : la mempool n'est plus une file unique ; elle est maintenant découpée en partitions logiques déterministes basées sur `transaction.from`, avec un nombre de partitions aligné sur le nombre de cœurs logiques disponibles et sans régression sur les 86 tests du projet.
- Étape 5.2 validée : la mempool est maintenant pilotée par des tâches asynchrones Tokio par partition, avec communication `mpsc`/`oneshot`, test dédié supplémentaire, et `cargo test` complet validé avec 87 tests passants.
- Étape 5.3 validée : le dispatcher réseau alimente maintenant la mempool partitionnée depuis `src/main.rs`.
- Étape 5.4 validée : le proposant de bloc pioche bien dans l’ensemble des partitions ; le test `propose_block_can_pull_transactions_from_all_partitions` le confirme explicitement, et `cargo test` passe désormais avec 88 tests validés.
- Sauvegardes locales créées pendant l’étape 5.4 :
  - `src/consensus/mod.rs.step-5.4.bak`
  - `journal_avancement.md.before_step_5_4_update`
- Sauvegardes locales créées pendant l’étape 5.3 :
  - `src/main.rs.step-5.3.bak`
  - `journal_avancement.md.before_step_5_3_update`
- Pense à sauvegarder les sources localement après chaque mise à jour importante (cf. fichier `sauvegarde.md`).
- Ne jamais stocker de clé privée, mot de passe ou token dans le code source, les variables d’environnement ou le terminal.
